{
  "command" : "ask",
  "envelope" : {
    "failure" : {
      "error" : {
        "code" : {
          "enum" : [
            "app_unavailable",
            "blocked_on_conflicts",
            "broker_unreachable",
            "human_declined",
            "ok",
            "repository_error",
            "request_failed",
            "session_terminated",
            "signing_failed",
            "timed_out",
            "usage"
          ],
          "type" : "string"
        },
        "hint" : {
          "optional" : true,
          "type" : "string"
        },
        "message" : {
          "type" : "string"
        }
      },
      "ok" : false,
      "schemaVersion" : 1
    },
    "success" : {
      "ok" : true,
      "result" : {
        "optional" : true,
        "schema" : "ask"
      },
      "schemaVersion" : 1
    }
  },
  "exitCodes" : [
    {
      "code" : 0,
      "meaning" : "The human picked an option; the payload is the ask reply (optionIndex, optionText, optional message)."
    },
    {
      "code" : 1,
      "meaning" : "Invalid arguments — no question, no --options, an empty option in the list, or a malformed --timeout."
    },
    {
      "code" : 3,
      "meaning" : "The Switchyard app is not running. Ask never launches the app."
    },
    {
      "code" : 5,
      "meaning" : "The app quit before the human decided — never reported as a decision."
    },
    {
      "code" : 7,
      "meaning" : "The human declined to answer; the payload still carries the declined reply with ok:true."
    },
    {
      "code" : 10,
      "meaning" : "No answer arrived within --timeout — a typed timeout outcome, never a decline and never an app failure."
    }
  ],
  "flags" : [
    {
      "argument" : "a,b,c",
      "help" : "The answer options, comma-separated, presented in this order. Required; an empty list or an empty option is a usage refusal.",
      "long" : "options",
      "short" : null
    },
    {
      "argument" : "seconds",
      "help" : "Give up the wait after this many seconds (default 3600). On expiry the CLI exits 10 with a typed timeout outcome — never a decline. A queued ask's timer starts when it reaches the head of its repository's queue.",
      "long" : "timeout",
      "short" : null
    }
  ],
  "schemaName" : "ask",
  "schemaVersion" : 1,
  "summary" : "Ask the human a question in the app and block until they pick an option, decline, or the wait times out."
}
