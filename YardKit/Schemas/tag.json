{
  "command" : "tag",
  "envelope" : {
    "failure" : {
      "error" : {
        "code" : {
          "enum" : [
            "app_unavailable",
            "blocked_on_conflicts",
            "broker_unreachable",
            "human_declined",
            "ok",
            "repository_error",
            "request_failed",
            "session_terminated",
            "signing_failed",
            "timed_out",
            "usage"
          ],
          "type" : "string"
        },
        "hint" : {
          "optional" : true,
          "type" : "string"
        },
        "message" : {
          "type" : "string"
        }
      },
      "ok" : false,
      "schemaVersion" : 1
    },
    "success" : {
      "ok" : true,
      "result" : {
        "optional" : true,
        "schema" : "tag"
      },
      "schemaVersion" : 1
    }
  },
  "exitCodes" : [
    {
      "code" : 0,
      "meaning" : "The tag was created; the payload carries the ref, the object it names (the tag object when annotated, the commit when lightweight), and whether it is annotated."
    },
    {
      "code" : 1,
      "meaning" : "Invalid arguments — tag requires exactly two positional arguments <name> <commit>; an unknown, duplicated, or value-missing flag; or both --sign and --no-sign."
    },
    {
      "code" : 4,
      "meaning" : "The tag could not be created for a reason the other codes do not name — an invalid name, an existing tag name, a `\/`-boundary clash with an existing tag, an unknown commit, a missing message on an annotated tag, a signing intent on a lightweight tag, or a signing failure among them."
    }
  ],
  "flags" : [
    {
      "argument" : null,
      "help" : "Create an annotated tag (implied by --message). Requires a message.",
      "long" : "annotate",
      "short" : null
    },
    {
      "argument" : "message",
      "help" : "The tag's message, passed as a flag — implies an annotated tag; GIT_EDITOR is never invoked.",
      "long" : "message",
      "short" : null
    },
    {
      "argument" : null,
      "help" : "Never sign, even when tag.gpgsign is true.",
      "long" : "no-sign",
      "short" : null
    },
    {
      "argument" : null,
      "help" : "Sign the annotated tag, even when tag.gpgsign is false.",
      "long" : "sign",
      "short" : null
    }
  ],
  "schemaName" : "tag",
  "schemaVersion" : 1,
  "summary" : "Create a lightweight or annotated tag at a commit."
}
