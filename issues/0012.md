# 0012 — Implement switchyard whereami

| | |
|---|---|
| **Status** | resolved |
| **Module** | yard / YardGit |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-06 |
| **Closed** | 2026-08-07 |
| **Commit** | `a795203` |

## Description

The single highest-value command in the project. An agent today spends four or five `git`
invocations and fragile text parsing to answer "where am I"; this replaces that preamble with one
call and one object.

Contents: branch, upstream, ahead/behind, in-progress rebase/merge/cherry-pick, stash count, dirty
paths, and conflict count.

**Scoped down 2026-08-06 after round 1 timed out.** Signing configuration moved to #0108, and the
sibling-worktree object to #0109. Both were independently useful and neither is needed to answer
"where am I" at all.

## Expected behavior

- [ ] New file `YardKit/Sources/YardGit/WhereAmI.swift`. Create it; do not restructure existing files.
- [ ] One call returns every field above; no field requires a follow-up command.
- [ ] Correct on a detached `HEAD`, mid-rebase, mid-merge, mid-cherry-pick, and in a linked worktree.
- [ ] Correct on an empty repository with no commits — a common agent starting state.
- [ ] Tested against both ref formats via the fixture harness (#0024).
- [ ] Tests use **swift-testing** — `import Testing`, `@Test`, `#expect`. Not XCTest. `swift test`'s summary line counts only swift-testing tests, so an XCTest file passes invisibly.
- [ ] `cd YardKit && swift build` succeeds, and `swift test` prints a `Test run with N tests` line you paste. N must be greater than the count on `main` before this change.

## Notes

Guide §6 "Read: structured state". Blocked by #0009, #0011.

## The API you must use — read these files before writing code

Round 1 wrote 679 lines against types that do not exist, and only opened `GitProcess.swift` in the
last five lines of its log. **Read `YardKit/Sources/YardGit/GitProcess.swift` and
`WorktreeContext.swift` first.** Everything below is verified.

**`GitProcess` is a concrete struct, not a protocol.**

```swift
public struct GitProcess: Sendable {
    public init(executablePath: String = "/usr/bin/git")
    public func run(...)      // -> Output
    public func capture(...)  // -> Output
}
```

There is **no** `GitProcess.GitRunner`, no `Platform`, and no injection seam. Construct it with
`GitProcess()` and call it. Do not invent an abstraction to make it testable — the tests run against
real repositories built by `FixtureRepository`.

**`WorktreeContext` has no memberwise init and no nested `WorktreeInfo`.** It is built by
`WorktreeContext.resolve(...)` and exposes `topLevel: String?`, `gitDir`, `commonDir`,
`worktreeName: String?`, `isLinkedWorktree`, `isBare`, plus `path(for:git:)` and `resolveRef(...)`.

**Never touch `.git/…` with `FileManager`.** Round 1 did this four times:

```swift
FileManager.default.fileExists(atPath: basePath + "/.git/rebase-merge")   // rejected
```

`rebase-merge`, `rebase-apply`, `MERGE_HEAD` and `CHERRY_PICK_HEAD` all come from
**`git rev-parse --git-path <name>`**, which is correct in a linked worktree and on a reftable
repository, where `.git/refs` does not exist at all. That is a hard project rule in `CLAUDE.md`, not
a preference.

## Fixture affordances that already exist

Do not build repository states by hand. `FixtureRepository` provides `build(_:)`, `branch(_:at:)`,
`checkout(_:)`, `checkoutDetached(_:)`, `addWorktree(named:branch:)`, `writeUntracked(_:)`,
`beginInterruptedRebase(onto:)`, `revParse(_:)`, `refNames()`, `isMidRebase`, `destroy()`, and
`RefFormat.supported(git:)` for the dual-format sweep.

## Review — round 1 timed out at the 1800s cap

Exit 143, nothing committed, and **no test file written at all** — the entire budget went on
production code that never compiled.

The cause was not the model's judgment. It read `WorktreeContext.swift` and `FixtureRepository.swift`
early, wrote the whole implementation, and then spent four rewrite cycles fighting compile errors
against `GitProcess.GitRunner` and `Platform` — types it had invented because **this issue never
named the collaborators it should use.** It named the output file and the field list and nothing
else.

Round 2 must be different in three ways:

1. **Read `GitProcess.swift` and `WorktreeContext.swift` before writing anything.** Their real
   surfaces are quoted above; if what you need is not there, stop and say so rather than inventing it.
2. **Write the test file alongside the implementation, not after.** A round with no tests is not a
   partial success, it is a failed round. If time is short, cover fewer fields with real tests rather
   than more fields with none.
3. **The scope is smaller now** — signing config and the sibling worktree are gone to #0108 and #0109.

Discard round 1's `WhereAmI.swift` entirely. It is built on types that do not exist.

## Review — round 2, and round 3 is the last

**The production code is close to right and should mostly be kept.** Round 2 used the real
`GitProcess` and `WorktreeContext`, invented nothing (`grep GitRunner|Platform|WorktreeInfo` → no
hits), obeyed the `.git/` rule, and returns correct values for every repository state — verified by
an independent probe calling `whereAmI` directly:

| state | branch | flags | counts |
|---|---|---|---|
| normal | `"main"` | — | 0/0/0 |
| linked worktree | `"probe-branch"` | — | 0/0/0 |
| bare | `"main"` | — | 0/0/0 |
| empty, no commits | `"main"` | — | 0/0/0 |
| detached | `nil` | — | 0/0/0 |
| mid-rebase | `nil` | midRebase | 0/0/0 |
| dirty | `"main"` | — | 2/1/0 |
| conflicted | `nil` | midMerge | 0/1/1, hasConflicts |

**It was rejected because the test file crashes the whole run** — signal 5, no
`Test run with N tests` line at all. Round 3 is a test-file repair.

### The cause was this issue, again

Round 1 named the output file and not its collaborators. The rewrite fixed that. But it then listed
`FixtureRepository`'s affordances **by signature only**, and round 2 spent its entire 1800s budget
rediscovering the semantics of exactly two of them. A signature says what you may call; it does not
say what the arguments mean.

### The four things to fix

1. **Commit names are keys in `repo.oids`, not refs.** `repo.revParse("a")` fails with
   `fatal: ambiguous argument 'a'`. Use `repo.oids["a"]!`. Same for `checkoutDetached(_:)` and
   `beginInterruptedRebase(onto:)`. **Never pass a bare commit name to git.**

2. **`addWorktree(named:branch:)` runs `git worktree add -b <branch>` — it CREATES the branch.**
   Passing `"main"` fails with `fatal: a branch named 'main' already exists`. Pass a new name.

3. **Never use `try!` in a test.** Mark the test `throws` and use `try`. Round 2 has six `try!`s, and
   the one at line 83 is why a single bad call took down the entire test process and destroyed the
   run summary. One failing assertion is a failing test; a trap is a lost round.

4. **`rawHead` on an empty bare repository returns the literal string `"HEAD"`.** Verified:
   `git -C <bare> rev-parse HEAD` prints `HEAD` and **exits 0**, so the `try?` guard never fires.
   Return `""` there, as the non-bare empty case already does.

### The empty-repository question — settled, do not re-decide

`git symbolic-ref --short HEAD` returns `"main"` on an unborn HEAD, so round 2's implementation
reports `branch: "main"` and `headOID: ""`. **That is correct and it stays.** Round 2's test expected
`nil` and was wrong.

The reasoning, since this is an agent-facing contract: an agent in a fresh repository *is* on `main`,
and will commit to `main`. Reporting `nil` would hide the one useful fact. `headOID == ""` is what
distinguishes unborn from checked-out, and it is already reported. **Fix the test, not the code.**

### Scope for round 3 — deliberately narrow

Change **`WhereAmITests.swift`** and, in `WhereAmI.swift`, **only** the `rawHead` bare-repo case from
item 4. Nothing else. Two rounds have died on breadth; this one is a repair.

Not in scope, filed separately: the conflict **count** the Description asks for (only `hasConflicts:
Bool` exists today) is #0110, and the stale doc comments are #0111.

### Verification for round 3

- [ ] `swift test` **completes and passes** — paste the line. N must be greater than 152. Round 2
      produced no line at all; a crash is a rejection.
- [ ] `grep -c 'try!' YardKit/Tests/YardGitTests/WhereAmITests.swift` returns **0**.
- [ ] The empty-repository test asserts `branch == "main"` and `headOID == ""`.
- [ ] `git rev-parse HEAD` on an empty bare repo is handled — `rawHead` returns `""`, not `"HEAD"`.
- [ ] `./scripts/check-tests-assert.sh` is clean.

## Work log

| Phase | Model | Tokens | Cost |
|---|---|---|---|
| Round 1 (timed out, invented API) | Opus 5 dispatcher | 37,875 | $0.30 |
| Round 2 (timed out, test file crashed) | Opus 5 dispatcher | 72,322 | $0.58 |
| Round 3 (accepted, 504s) | Opus 5 dispatcher | 51,411 | $0.41 |
| Implementation (three rounds) | Ornith 1.0 35B-A3B, local via LM Studio | — | **$0.00** |
| **Total** | | **161,608** | **$1.29** |

Three rounds, and the first two failed on **this issue**, in two different ways that are worth
keeping separate:

- **Round 1** named the output file and none of its collaborators. The model invented
  `GitProcess.GitRunner` and `Platform` as an injection seam and never opened `GitProcess.swift`
  until the last five lines of its log.
- **Round 2** was given the real API and used it correctly — the production code was accepted on its
  merits and survives to `main` essentially unchanged. But the issue listed `FixtureRepository`'s
  affordances **by signature only**, and the round spent its whole budget rediscovering that `"a"` is
  a key in `repo.oids` rather than a ref, and that `addWorktree` *creates* the branch it is given.
- **Round 3**, told those two facts and scoped to a repair, converged in 504s.

The pattern across all three: *a signature says what you may call; it does not say what the arguments
mean.* Both failures were the author's, one level apart.

One design question was settled here rather than left to the implementer: an empty repository reports
`branch: "main"`, not `nil`. An agent in a fresh repo is on `main` and will commit to `main`, and the
empty `headOID` already distinguishes unborn.

Coverage gap found in review and deliberately not widened into: #0112.

