# 0112 — WhereAmI has no real mid-merge or mid-cherry-pick coverage

| | |
|---|---|
| **Status** | resolved |
| **Module** | YardGit |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-07 |
| **Closed** | 2026-08-07 |
| **Commit** | `b8ecba3` |

## Description

`midMergeReportsFlagWhenMergeHeadPresent()` in
`YardKit/Tests/YardGitTests/WhereAmITests.swift` asserts nothing at runtime, and its name says
otherwise.

It merges `repo.oids["a"]!` — which is an **ancestor** of the current commit. Git prints
`Already up to date.` and exits 0, so `MERGE_HEAD` is never created. The test then hits
`guard let mPath = …, !mPath.isEmpty else { return }` and exits before reaching its single `#expect`.
It passes, every time, having tested nothing.

Even if the guard were reached, the assertion compares `r.isMidMerge` against the same `FileManager`
check the test just performed — so it would confirm that two readings of the same fact agree, not
that the implementation is right.

**And there is no mid-cherry-pick test at all**, though the Expected behavior of #0012 names it.

## Givens — verified on `main`, treat as true

1. The file is `YardKit/Tests/YardGitTests/WhereAmITests.swift`; the implementation under test is
   `YardKit/Sources/YardGit/WhereAmI.swift`, which is **correct** — it reports `isMidMerge` and
   `isMidCherryPick` from `git rev-parse --git-path`. Do not change it.
2. **Merging an ancestor is a no-op.** To create `MERGE_HEAD` you need two commits that have actually
   diverged: branch from a common base, commit on each side, then merge with a conflict or with
   `--no-commit` so the merge stays in progress.
3. `FixtureRepository` gives `build(_:)` with per-commit `parents` and `files`, `branch(_:at:)`,
   `checkout(_:)`, and `writeUntracked(_:)`. Commit names like `"a"` are keys in **`repo.oids`**, not
   refs — passing a bare name to git fails with `ambiguous argument`.
4. **Never `try!` in a test.** Mark the test `throws` and use `try`. A trap destroys the whole run
   summary, which cost #0012 a round.
5. `scripts/check-tests-assert.sh` flags a bare `return` in a `@Test` body as SKIPSHAPE. It is
   advisory today only because instances like this one exist.

## Expected behavior

- [ ] The mid-merge test creates a **real** in-progress merge — divergent commits, `MERGE_HEAD`
      present — and asserts `isMidMerge` is true. No early `return`.
- [ ] It asserts against the **function's output**, not against a `FileManager` check the test
      performs itself. A test that re-reads the same file the implementation reads proves only that
      the file exists.
- [ ] A **mid-cherry-pick** test exists and asserts `isMidCherryPick` is true, created by
      cherry-picking a commit that conflicts so the operation stays in progress.
- [ ] Both assert the *other* flags are false — a mid-merge must not report mid-rebase.
- [ ] No bare `return` in either test.
- [ ] Tests use **swift-testing** — `import Testing`, `@Test`, `#expect`. Not XCTest.
- [ ] `cd YardKit && swift build` succeeds and `swift test` prints a `Test run with N tests` line you
      paste. **N must be greater than 216**, the count on `main` before this change.

## Tests must be able to fail

Prove each new test binds: make `isMidMerge` return a constant `false`, confirm the mid-merge test
fails, restore. Do the same for `isMidCherryPick`. A test that still passes with the field stubbed is
the exact defect this issue exists to remove.

## Notes

Found during review of #0012 round 3, which was scoped to a test repair and correctly did not widen
to cover this.

Once this and #0105 have both landed, SKIPSHAPE can be promoted to a hard failure in
`check-tests-assert.sh`. Whichever lands second should do it.

## Review — round 1 reinvented a helper that already exists, badly

The suite does not complete. It aborts with `signal code 5` and **no `Test run with N tests` line at
all**:

```
WhereAmITests.swift:98:  Fatal error: Unexpectedly found nil while unwrapping an Optional value
WhereAmITests.swift:143: Fatal error: Unexpectedly found nil while unwrapping an Optional value
```

Both mutations were therefore **unrunnable**, which is a worse outcome than a failed mutation: the
tests trap at lines 98 and 143, *before* they ever call `whereAmI()`. They cannot distinguish a
correct implementation from a constant `false`, because they never reach it.

### 1. `FixtureRepository.branch(_:at:)` does not populate `oids`

`Sources/YardGit/FixtureRepository.swift:150` creates a ref and writes nothing to `oids` — only
`build(_:)` does, at line 134. So `repo.oids["branch1"]!` and `repo.oids["branch2"]!` are
**unconditionally nil** and trap on every run.

**Only commits created by `build(_:)` appear in `oids`.**

### 2. `FixtureRepository.conflicted()` already builds the state this issue needs

`Sources/YardGit/FixtureRepository.swift:296`. It creates `base`, then divergent `ours` and `theirs`
both editing `f.txt`, checks out `ours` detached, and runs `merge --no-commit` on `theirs` — leaving a
**real in-progress conflicting merge with `MERGE_HEAD` present**.

That is exactly the fixture the mid-merge test requires. **Use it. Do not build your own.** Read it
and follow the same divergent-commit shape for the cherry-pick test.

This is the round's real failure: it reconstructed, incorrectly, something the codebase already
provided. The fault is partly this issue's — it named the *constraint* ("merging an ancestor is a
no-op") without naming the *affordance* that satisfies it.

### 3. The tests still build only one commit

Both build `"base"` and then point `branch1` and `branch2` at it. That is not divergence. Even with
the nil traps fixed, the merge would print `Already up to date.`, `MERGE_HEAD` would never exist, and
the test would be exactly as vacuous as the one this issue was filed to replace.

**Two commits must exist with a common parent and conflicting content in the same file** before any
merge or cherry-pick can remain in progress.

### Also

Round 1 kept redundant `FileManager.default.fileExists` assertions alongside the real ones. Once the
tests actually bind, delete them — re-reading the file the implementation reads proves only that the
file exists.

### Verification for round 2

- [ ] `swift test` **completes** and prints a `Test run with N tests` line. **N must be > 216.** Round
      1 produced no line at all.
- [ ] `grep -c '!' ` on the new test code shows no force-unwrap of anything from `oids` that
      `build(_:)` did not create.
- [ ] **Mutation 1**: `isMidMerge` stubbed to `false` → the mid-merge test FAILS.
- [ ] **Mutation 2**: `isMidCherryPick` stubbed to `false` → the cherry-pick test FAILS.
      Both must be *runnable*; a trap is not a failure.
- [ ] The fixture genuinely has `MERGE_HEAD` (and `CHERRY_PICK_HEAD`) present — say how you confirmed
      it, from the function's output rather than from a `FileManager` check the test performs itself.
- [ ] `git diff -- YardKit/Sources/` is empty. `WhereAmI.swift` is correct and must not change.

## Review round 2 — the merge test is fixed; the cherry-pick test was never touched

The suite completes and both mutations are now **runnable**, which was round 1's blocking defect:

```
✔ Test run with 217 tests in 16 suites passed after 3.502 seconds.
✘ isMidMerge → false      : midMergeReportsFlagWhenMergeHeadPresent      FAILED correctly
✘ isMidCherryPick → false : midCherryPickReportsFlagWhenConflictPresent  FAILED correctly
```

The merge fixture is genuinely right: `base → branch1` and `base → branch2` with conflicting `f.txt`,
`MERGE_HEAD` present, **3 unmerged index entries**, `merge-base branch1 branch2 == base`.

**The cherry-pick fixture is still round 1's.** The round said so in its own log: *"Cherry-pick test
was NOT yet touched in this round."* It replaced the nil force-unwrap with a `rev-parse`, which
stopped the trap without fixing the fixture. Replicated with raw git: `branch1 == branch2 == base`,
**total commits: 1**, and **0 unmerged index entries**.

`CHERRY_PICK_HEAD` exists anyway — cherry-picking a commit whose content the tree already has
produces an **empty** commit, and git parks there with a non-zero exit and prints *"The previous
cherry-pick is now empty… nothing to commit, working tree clean."* So the test passes by accident of
git's empty-commit handling, not because a cherry-pick is mid-conflict. Its own name says
`...WhenConflictPresent`; its comment says "commits edit the same line". Neither is true.

### The exact fixture, verified with real git on 2026-08-07

Do not derive this. Run of the shape below produced `exit=1`, `CHERRY_PICK_HEAD` present,
**3 unmerged index entries**, `git diff --name-only --diff-filter=U` → `f.txt`, 3 total commits,
and `merge-base branch1 branch2 == base`:

```
base:  f.txt = "original\n"
a1:    parent base, f.txt = "ours\n"       ← checked out
a2:    parent base, f.txt = "theirs\n"
git cherry-pick <a2>                        ← conflicts
```

In `FixtureRepository` terms — this is `conflicted()` at `FixtureRepository.swift:296` with
`cherry-pick` substituted for `merge`, which is why the review said to follow it rather than invent:

```swift
var repo = try FixtureRepository()
try repo.build([Commit("base", files: ["f.txt": "original\n"])])
try repo.build([Commit("a1", parents: ["base"], files: ["f.txt": "ours\n"])])
try repo.build([Commit("a2", parents: ["base"], files: ["f.txt": "theirs\n"])])
try repo.checkoutDetached(repo.oids["a1"]!)
_ = try? GitProcess().capture(["cherry-pick", repo.oids["a2"]!], workingDirectory: repo.url.path)
```

All three keys come from `build(_:)`, so those subscripts are safe. `capture` rather than `run`
because a conflicting cherry-pick exits non-zero and `run` throws.

### Verification for round 3 — this is the last round

- [ ] **`git ls-files -u` in the cherry-pick fixture reports more than zero entries.** This is the
      criterion that separates a real conflict from the empty-commit accident, and it is the only one
      round 2 would have failed. Assert it in the test — do not merely check that `CHERRY_PICK_HEAD`
      exists, because it exists in both states.
- [ ] The cherry-pick fixture contains **3 commits**, and `merge-base` of the two branches is `base`.
      Report both numbers from the fixture, not from a shell replica.
- [ ] Both mutations still runnable and still failing. Report each separately.
- [ ] `swift test` completes with **N > 217**, or exactly 217 if no test was added — say which and why.
- [ ] `git diff -- YardKit/Sources/` empty. `WhereAmI.swift` is correct and must not change.
- [ ] Delete the `FileManager.default.fileExists` assertions. Round 2 kept them, hoisted into
      `mergeHeadExists` / `cherryPickExists` locals. Re-reading the file the implementation reads
      proves only that the file exists — the implementation's own returned flag is the assertion.

## Work log

| Phase | Model | Tokens | Cost |
|---|---|---|---|
| Round 1 (rejected, tests trapped before reaching the function) | Opus 5 dispatcher | 51,204 | $0.41 |
| Round 2 (rejected, cherry-pick fixture untouched) | Opus 5 dispatcher | 53,571 | $0.43 |
| Round 3 (accepted) | Opus 5 dispatcher | 54,826 | $0.44 |
| Implementation (three rounds) | Ornith 1.0 35B-A3B, local via LM Studio | 1,616,580 (1,595,867 in / 20,713 out) | **$0.00** |
| **Total billed** | | **159,601** | **$1.28** |

Three rounds for a test-only change, and each round failed differently — which is why it is worth
writing down.

**Round 1** force-unwrapped `repo.oids["branch1"]!`, a key `branch(_:at:)` never writes, so the tests
trapped at lines 98 and 143 *before* calling `whereAmI`. Both required mutations were therefore
**unrunnable**, which is strictly worse than a failed mutation: the tests could not distinguish a
correct implementation from a constant `false`, because they never reached it. The issue is partly to
blame — it named the constraint ("merging an ancestor is a no-op, you need divergent commits") without
naming the affordance, `FixtureRepository.conflicted()`, that already builds exactly that state.
Checklist item 17.

**Round 2** fixed the merge fixture properly and said in its own log that it had not touched the
cherry-pick one. It passed anyway, because the test asserted that `CHERRY_PICK_HEAD` exists — and that
is true in **two** different states. Cherry-picking a commit whose content the tree already has
produces an *empty* commit, and git parks at `CHERRY_PICK_HEAD` with a non-zero exit either way. The
assertion could not tell the state it wanted from the state it got. That is checklist item 19, and it
is a sharper form of #0106's unfalsifiable predicate: not "can this fail" but "what *else* makes this
pass".

**Round 3** got the fixture handed to it as verified Swift rather than as a requirement, and converged
in 250s. The reviewer confirmed the new assertion discriminates by replicating round 2's fixture and
watching it fail there — which is the only way to know a repair actually repairs.

The count moved 217 → 222 across the round only because merging `main` brought in #0110's and #0095's
tests; no test was added or removed in round 3.

