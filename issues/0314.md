# 0314 — Trailer.parse's colon split direction is unasserted

| | |
|---|---|
| **Status** | resolved |
| **Module** | YardGit |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-18 |

## Description

**Finding 5 of the M1 milestone review, twelfth pass, 2026-08-18.** `Trailer.parse` splits on the
**first** colon, and no test carries a colon in a trailer *value* — so the direction is unasserted.

`YardKit/Sources/YardGit/CommitLog.swift:72`, mutated `trimmed.firstIndex(of: ":")` → `lastIndex(of: ":")`:
**full suite green, 1115 tests.**

**Probe, run both ways:**

| source | `Trailer.parse("Claude-Session: https://claude.ai/code/session_013q")` |
|---|---|
| pristine | `Claude-Session: https://claude.ai/code/session_013q` |
| mutant | **`nil`** — the key candidate now contains whitespace, so the guard rejects it and the trailer is dropped |

**This project's own commit trailers carry that exact URL shape** — `CLAUDE.md`'s commit-message rule
requires a `Claude-Session:` trailer with a URL in it. So the direction is load-bearing on this
repository's own history and entirely unasserted.

## Expected behavior

- [ ] A named test asserting `Trailer.parse` keeps the whole value when it **contains a colon** — the
      `Claude-Session: https://…` shape is the natural case and the one that matters here.
- [ ] Assert through `CommitLog.run` as well if it is cheap, so the wiring is covered and not only the
      parser. Say either way.
- [ ] **Mutation:** `firstIndex` → `lastIndex` at line 72. The new test must redden.
- [ ] `cd YardKit && swift test` **twice, in the foreground with an explicit 900000 ms timeout**, both
      green, above the count in `docs/test-baseline.txt`.

## The files

`YardKit/Tests/YardGitTests/CommitLogTests.swift`. **No production change** — `CommitLog.swift:72` is
correct.

## Notes

**Milestone M1**, criterion 5, one of seven from the twelfth pass. Class (d) of the review's proposed
restatement: *a scan whose direction is load-bearing needs an input with at least two candidates.*

## Work log

- **Round 1 — 2026-08-18, Sonnet subagent, 80,416 tokens, 6m30s, accepted.** Two tests. 1126 → 1128.
- **It read the current source rather than the issue's line numbers**, which mattered: #0313 had
  rewritten `parseTrailerBlock` hours earlier, so the end-to-end fixture needed a blank line before the
  trailer to satisfy the new last-paragraph rule.
- **It covered the `run` path as well as the parser**, which the issue offered as optional-if-cheap — and
  that is the half that reddens on both ref formats under the mutation.
