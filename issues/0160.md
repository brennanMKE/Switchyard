# 0160 — Attach captured post-rewrite mappings to journal entries

| | |
|---|---|
| **Status** | open |
| **Module** | YardGit |
| **Milestone** | M2 |
| **Platform** | macOS |
| **First seen** | 2026-08-07 |

## Description

The storage half of the original #0043, split off when #0043 was re-authored to the
decision core. #0043's `PostRewrite.decide` turns one hook invocation into a
`Decision` — source (`amend`/`rebase`), `isOwnInvocation`, the ordered
`[Rewrite]` mapping, and a malformed-line count. This issue persists that:

- **Own invocations** (`isOwnInvocation == true` — the marker `GitProcess` sets)
  attach the mapping to the **in-flight journal entry** for the `switchyard`
  operation that ran the rewrite. This is why #0043 routes rather than skips:
  for `switchyard`'s own rebases the hook is the *only* source of the mapping.
- **Foreign invocations** become **observed** entries, the same class #0153
  defines for foreign ref transactions, so `undo` never offers to revert them.
- Only the **final `rebase` invocation** of a rebase is authoritative: measured
  on git 2.50.1 (#0043 Givens), squash/fixup/reword steps raise extra `amend`
  invocations mid-rebase whose old oids are intermediate commits that never
  existed in the pre-rewrite history. Persistence must not double-record a
  rebase from its mid-flight `amend` invocations.
- A **dropped commit appears in no mapping line at all** (measured — absence is
  the representation). Naming drops explicitly requires the pre-rewrite commit
  set from the journal entry's snapshot; that comparison lives here, not in the
  parser.

## Expected behavior

- [ ] An `amend`/`rebase` `Decision` from a foreign rewrite lands as an observed
      journal entry carrying source, the ordered old→new pairs, timestamp, and
      worktree, marked so `undo` never offers to revert it.
- [ ] A `Decision` with `isOwnInvocation == true` attaches its mapping to the
      corresponding in-flight journal entry instead of creating an observed one.
- [ ] Mid-rebase `amend` invocations do not double-record a rebase.
- [ ] Failure to persist never surfaces as a non-zero hook exit — #0043's
      totality invariant holds through this layer.
- [ ] #0064 (`rewrite-diff`) can read the stored mapping to compute its ranges.
- [ ] `swift test` prints a `Test run with N tests` line; N must exceed the
      pre-change baseline (re-measure at planning time; state the delta).

## Notes

Blocked by #0027, #0028, #0043. Sibling of #0153 (observed entries for foreign
ref transactions). The process arm the installed `post-rewrite` script execs is
the same layering decision as #0154's ref-txn arm and must be settled with it,
not improvised here. To be re-authored to code level when it reaches the front
of the queue.

## Planning note — 2026-08-17. Four deliverables, and one the store cannot hold yet.

**Not dispatched: this issue is oversized as written**, and the sizing rule (one verb per issue) is
the one whose violation shows up as a round getting partway into each of several pieces — #0010,
#0011 and #0012 all failed that way. The Expected behavior list has four independent deliverables
with different failure modes:

1. a foreign rewrite becomes an observed entry;
2. an **own** invocation attaches its mapping to the in-flight journal entry;
3. mid-rebase `amend` invocations do not double-record a rebase;
4. persistence failure never produces a non-zero hook exit.

(4) is the same invariant #0191 is closing for ref transactions and should be settled the same way,
in whichever issue lands first. (3) is a deduplication rule whose correctness depends on (2).

### The design point that has to be settled before any of it is planned to code level

**`JournalObserved` cannot carry a rewrite mapping today.** #0153 landed
`JournalObserved.Metadata` with exactly one payload field:

```swift
public let updates: [ReferenceTransaction.RefUpdate]
```

A rewrite is not a ref update — `PostRewrite.Rewrite` is an old→new **commit** pair
(`PostRewrite.swift:61`), and a `Decision` also carries a `source` of `.amend` or `.rebase`
(`:39`, `:106`). So deliverable (1) needs one of:

- **a second payload field** on `Metadata`, optional, alongside `updates` — cheap, but every observed
  entry then carries a shape that is empty for the other kind, and the wire format grows a variant
  that must be pinned in both directions;
- **a payload discriminator** — `Metadata` becoming an enum over `refUpdates` / `rewrites` — honest
  about the two kinds, and a wire change to a format that has existed for one day;
- **a separate ref namespace per kind**, e.g. `refs/switchyard/observed/rewrites/`, which keeps each
  metadata shape single-purpose and costs a second `list`.

I have not picked one. All three are defensible and the choice changes what gets built, so it belongs
in a planning pass with the wire tests in view — not in a note written while two rounds are in flight.

### What (2) needs that does not exist

"The **in-flight journal entry** for the `switchyard` operation that ran the rewrite" presumes a way
to identify that entry from inside a hook. Nothing in `YardGit` exposes one today: `JournalAnchor`
mints an id at write time and the hook runs *during* the operation, before the entry is written. That
is a real mechanism to design — an environment variable carrying the id, a lock-file convention, or
recording the mapping first and attaching it afterwards — and it is the substance of deliverable (2),
not an implementation detail of it.

**Next step:** split into at least three issues once the payload question is settled, with (4) folded
into whichever lands first. Do not dispatch this issue as written.

## Split into #0220 and #0221 — 2026-08-17. This becomes the umbrella.

The planning note above said this issue is oversized as written and named the two things blocking it.
Both are now answered, and the work is filed as children:

- **#0220 — foreign rewrites become observed entries.** Carries the payload question's answer: a
  **`kind` discriminator added additively** to `JournalObserved.Metadata`, one namespace, no
  `schemaVersion` bump. Of the three readings listed above, a second optional field leaves both kinds
  ambiguous and a second ref namespace costs a `list` on every read for a distinction a key already
  makes. Deliverable (3) — mid-rebase `amend` invocations must not double-record — goes here, because a
  **foreign** rebase raises them too. Deliverable (4), the totality invariant, goes here as well, since
  this is the half that lands first.
- **#0221 — own invocations attach to the in-flight entry.** Carries the mechanism question's answer:
  `JournalCheckpoint.around` **writes the entry before running its body**, so the id exists before any
  git subprocess starts and can be exported through the environment the same way the own-invocation
  marker already is. That is a variable, not a design.

This issue stays open as the parent and is reviewed once both resolve, per CLAUDE.md's umbrella rule.
Do not dispatch it directly.

## Umbrella review — 2026-08-17. Not clean: four findings, three filed, one scope clarification.

Run by an Opus subagent against this issue's own criteria, in a `git archive` scratch copy (1010
green there). Both children were merged at the time.

- **Bullet 3 — mid-rebase amends must not double-record — is met on the foreign path only.** The gate
  sits after `guard !decision.isOwnInvocation`, so it is structurally unreachable for own invocations,
  and `attachRewrite` has no equivalent. A real own autosquash attached a mapping whose old oid is an
  intermediate commit. Filed as **#0233**.
- **Bullets 2 and 5 — one operation, three invocations, one storage slot.**
  `Fixup.run(source:target:)` fires three `post-rewrite` invocations under one entry id and
  `attachingRewrite` replaces rather than appends, so the pair naming the entry's own pre-operation
  `HEAD` is lost. Filed as **#0234**.
- **Bullet 1 — timestamp and worktree are absent** from `JournalObserved.Metadata`. Filed as
  **#0235**, and decided there: add both rather than narrow the criterion.
- **Bullet 4 — the totality invariant — is met by nothing, and cannot be met here.** See the
  clarification below.

### Bullet 4 moves to #0154 — a scope clarification, 2026-08-17

The criterion is *"failure to persist never surfaces as a non-zero hook exit"*. The review established
that there is **no `PostRewrite.runHook`**: `grep runHook Sources/` returns one hit, in
`ReferenceTransaction.swift`, which is the shape the criterion describes and exists only for
`reference-transaction`. So the invariant has no caller to hold it, and this issue's only test for it
asserts `decision.exitCode == 0` after a failed write — a stored property `record` cannot touch, which
#0220's own work log already admitted was weak.

**The storage is M2's; the hook arm that must never exit non-zero is not.** `PostRewrite.runHook` is
the process arm, and the process arm is **#0154**, which is M3 and blocked on **#0217**. Bullet 4 is
therefore recorded as **#0154's acceptance criterion**, in the same shape as
`ReferenceTransaction.runHook`: catch every throw, return `exitCode: 0`, carry a `recordingFailure`
string, and test it with a `GitProcess` that cannot write.

Same reasoning as guide §11 decision 17 — a milestone's criterion may not be gated on a later
milestone's work. This is not the criterion being dropped; it is being recorded where it can be met.

**#0160 closes when #0233, #0234 and #0235 resolve and a second review is clean.**

## Second umbrella review — 2026-08-17. Not clean: two findings, one proposal.

All three fixes from the first review were re-checked by mutation in a `git archive` scratch copy and
each reddened the test written for it — #0233's gate deletion reddens
`aMidRebaseAmendAttachesNothingToItsInFlightEntry`, #0234's last-write-wins reddens
`attachingAnExistingCommitFixupMappingKeepsThePreCheckpointHeadReachable`, #0235's date-strategy
deletion reddens `aForeignRewriteRecordWritesTheExactPinnedBytes`. Bullets 1 and 3 are met.

Two things the composition work exposed, both filed:

- **#0237 — an interrupted `fixup` stores no mapping anywhere.** `Fixup.run` leaves a conflicted rebase
  in progress by design, so `around` unwinds and the scoped `GitProcess` carrying the entry id stops
  existing; whatever continues the rebase is an own invocation **with no id**, and both halves refuse
  that quadrant. Bullets 2 and 5.
- **#0236 — a foreign rewrite's mapping is write-only.** `JournalObserved.Metadata` is `Encodable`
  only, and `JournalAnchor.metadata(for:in:git:)` hardcodes the journal namespace, so nothing in-tree
  can read an observed entry back. Bullet 5, which #0064 depends on.

The review also confirmed independently that **bullet 4's move to #0154 was legitimate**: `grep runHook
Sources/` finds only `ReferenceTransaction.runHook`, so the invariant has no caller in this milestone,
and #0160's test for it would stay green if `record` deleted the repository.

**Proposal filed as #0238**: the `ref_updates` kind's ISO-8601 encoding is unpinned — deleting the
strategy from that overload leaves 31 tests green. It is #0153's surface, so it is not a finding
against this umbrella and does not block it.

**#0160 closes when #0236 and #0237 resolve and two consecutive reviews are clean.** This was the
first review after the fixes and it was not clean, so the count starts over.

## Third umbrella review — 2026-08-17. Not clean: two findings.

Both fixes from the second review were re-checked by mutation and each reddened a named test —
#0236 by pointing `JournalAnchor.metadata` at `refPrefix` and by dropping `Decodable` (which fails to
compile in the tests, itself a kind of pin), #0237 by deleting the file write and by deleting the
staleness guard. Bullets **1** and **3** re-confirmed met.

- **#0241 — the in-flight file is an unkeyed slot with no operation lifetime.** A defect in guide §11
  decision 19 as I wrote it: the file names an entry rather than an in-progress operation, so an
  abandoned fixup's file is consumed by a later unrelated rewrite, and a successful command in between
  erases the mapping entirely. `Fixup`'s own abort arms produce that state. Bullet 2. Decision 19 has
  been amended.
- **#0242 — observed metadata still has no production decoder.** #0236 made the type `Decodable`, but
  the only decoder with a matching `dateDecodingStrategy` lives in a test. Bullet 5 — the same "the
  knowledge lives only in a test" shape #0236 was filed to remove, one step further in.

**The count starts over again.** #0160 closes when #0241 and #0242 resolve and two consecutive reviews
are clean.

## Fourth umbrella review — 2026-08-17. Not clean: three findings, all bullet 2.

Sixteen mutations plus five probes of the new #0241 state. **Bullets 1, 3 and 5 are met**, and **no
earlier pin has gone stale** — every test written to pin a bullet in this umbrella still dies to a
mutation of the behaviour it names, which is the question that caught #0230's regression in the
sibling umbrella.

- **#0253** — `inFlightEntryID` validates only that the file's id is a live *entry*, never that the
  operation is still in progress. So a user's own `git rebase --abort` — the documented alternative to
  continuing — leaves the slot, and a later unrelated rewrite consumes it. The read path implements
  the sentence decision 19 was amended to reject.
- **#0254** — `around`'s guard asks whether *a* resumable operation is live, not whether *this body*
  left one, so a second operation that **fails** during someone else's rebase claims the slot. The
  uncovered quadrant is the common one: while a rebase is in progress most operations fail, because
  the index is unmerged.
- **#0255** — three `attachRewrite` decisions nothing pins: the own/foreign guard (its mirror in
  `record` **is** pinned), environment-wins precedence, and removing the file after a successful
  attach.

**Bullet 5 is met**, and the review demonstrated it end to end with no `@testable`: the own path via
`JournalList.list` → `metadata.rewrite`, the foreign path via `JournalObserved.list` →
`JournalAnchor.metadata(for:in:namespace:git:)` → `Metadata(serialized:)`. #0242 closed what it was
filed for.

**Proposals, not filed as findings:** a one-call `JournalObserved.metadata(for:in:git:)` so a consumer
need not know to pass the namespace, and — measured — **`Fixup.clearInFlightFile` is now unexercised**
(deleting both call sites leaves the suite green). #0253 is told to delete it if its change makes it
genuinely redundant.

**The count stays at zero.**

## Fifth umbrella review — 2026-08-18. Not clean: one finding, reproduced by probe.

**#0264**, and it is the fourth issue against one root cause. The slot now has a claim rule (#0254), a
release rule (#0261) and a read rule (#0253) — **all three ask a question about the worktree and none
about the call**, so they compose correctly only when every rebase was started inside an `around`. A
rebase started outside one, which is the ordinary case this mechanism exists for, is invisible to all
three. The review reproduced two routes against pristine `main`: a foreign rebase's mapping attaching
to an unrelated failing operation's entry, and a stale slot surviving #0261's clear because a *foreign*
sequencer was live at the moment it would have fired.

**Ten mutations; nine killed a named test.** Including the one the task specifically asked about:
mutation 5 replaced the `stillInProgress: nil` default's re-probe with a constant and
`aSuccessfulAttachThroughTheFileRemovesIt` died — that test stands up a real `rebase-merge` directory
rather than injecting the answer, so #0253's injection pattern did not leave a hole this time.

**Two proposals, neither filed.** `JournalObserved.isMidRebaseAmend` checks only `rebase-merge` while
`SequencerSnapshot.capture` checks both layouts — the review could not construct a reachable mid-rebase
`amend` under `--apply`, so it is not a violation today, but the two probes now disagree about what "a
rebase is in progress" means in a file where that disagreement is the subject of three issues. And
`AppConnectionTests` went red under the extra load of a mutated run — the known #0240 class, unrelated
to this umbrella, but it will make some future round look red for the wrong reason.

**Clean-review count: 0.** Restarts when #0264 resolves.

## Sixth umbrella review — 2026-08-18. Not clean: three findings, and the mechanism is being replaced.

**Sixteen mutations; thirteen killed a named test. The three survivors are the story.**

**#0273 — the fifth wrong-entry attach, and the last one to be patched.** #0264's `orig-head` stamp does
not identify an operation: `git rebase --abort` restores `HEAD` to exactly `orig-head`, so a retry from
the same tip carries the identical stamp. The review reproduced it by taking #0264's own test and
removing the extra commit that test manufactures **precisely to force the two apart** — its comment says
so. Every rule tried so far has been a proxy for operation identity, and every proxy has turned out
non-unique. So **guide §11 decision 24** stops guessing the lifetime and takes git's: the entry id moves
inside `rebase-merge/` / `rebase-apply/`, which git deletes on both finish and abort. Measured, with our
file present, including that `rebase --continue` is unaffected.

**#0274 — two pins went stale without anyone touching them.** #0261's clear and #0253's `stillInProgress`
are both still correct and both now survive deletion, because #0264's changes made a neighbouring guard
fail first. One of them, `stillInProgress`, was recorded as **live by the fifth review** five hours
earlier. That is a new failure shape for this project: *a pin can rot without its test or its behaviour
being edited*, and it is now something these reviews look for deliberately.

**The questions the review was asked, all answered by measurement:** `git am` creates `rebase-apply`
with **no `orig-head`** (safe degradation today, irrelevant after #0273); a conflicted `git cherry-pick`
creates no sequencer directory at all, so nothing is claimed or read; the slot path really is
per-worktree (`.git/worktrees/<name>/switchyard/…` from a linked worktree); and both #0264 tests' foreign
rebases really are foreign — raw `Process` with the marker variables stripped.

**One proposal, recorded not filed:** `RepositoryLayout.swift:49-50` still documents the slot's contents
as *"the entry id's `description`, nothing else"*, which #0264 changed. #0273 will move the file
entirely, so the comment gets corrected there rather than in its own issue.

**Clean-review count: 0.** Restarts when #0273 and #0274 resolve.

## Seventh umbrella review — 2026-08-18. **CLEAN.**

**Fifteen mutations; fourteen killed a named test.** The survivor is a proposal the fifth review already
recorded, not a new violation. **No sixth instance of the wrong-entry root cause — decision 24 holds.**

**The review looked for a new shape rather than the old one**, which is the right instinct after five
instances of one root cause, and it constructed and rejected four routes: two `around` calls sharing a
sequencer (blocked by `sequencerWasLiveBefore`, proved by mutation); nested `around` (no call site
nests — four checked); the environment id winning over an older file (no in-tree command runs
`rebase --continue`); and `git rebase --quit`, **measured** to remove the directory with our file in it,
so the third teardown verb behaves like the other two.

**Each of the five deletions was checked against the case it used to guard**, and `stillLive` was
confirmed both load-bearing and pinned. **No earlier pin has gone stale** — the question that produced
#0274 last pass was asked again and every prior mutation still kills the same named test.

**Two proposals were acted on immediately** rather than filed, because both were documentation:
decision 24 had been appended *after* `## Reference material`, outside the numbered list that fifteen
source comments cite as their authority — moved under `### Settled`. And its lifetime claim needed a
qualification: **the file lives inside the region `SequencerSnapshot` captures**, so a checkpoint taken
mid-operation captures it and restore re-materializes it. That is consistent rather than wrong — file
and sequencer travel as one tree — but it is the one route by which the record can reappear, and it is
the reason `stillLive` survived the deletions.

**One proposal became #0286**: an unguarded `try` on the entry-id write replaces the body's error, so a
failed write can discard a `blockedOnConflicts` classification the caller needs.

**Two proposals recorded and not filed.** `isMidRebaseAmend` still probes only `rebase-merge` while the
file mechanism now spans both layouts — still unreachable, but the *stakes* changed, since `around` now
writes into `rebase-apply/` too. And the concurrent-`around` race in one worktree is analytically
reachable and untested — but nothing in `YardGit` locks a worktree anywhere, so it belongs to a
repository-locking issue, not to this umbrella.

**Clean-review count: 1.** One more clean pass closes #0160.

## Eighth umbrella review — 2026-08-18. Not clean: one finding, and it overturns three prior passes.

**#0291.** `isMidRebaseAmend` probes only `rebase-merge`, and passes 5, 6 and 7 each recorded that as a
proposal on the grounds that *"`git rebase --apply` never amends internally."* **True, and not what
bullet 3 says.** The criterion is about mid-rebase `amend` *invocations*, not about who raises them —
and a user's own `git commit --amend` during a stopped `--apply` rebase raises one. The eighth pass
measured it end to end on real git and then against production code: **two observed entries for one
rebase**, the second carrying an intermediate oid that never existed in the pre-rewrite history.

**The lesson is about reviews, not about this bug.** Three passes reached the same wrong conclusion
because each re-examined the *argument* rather than the *criterion*. A proposal that survives three
passes unchanged is a candidate for being measured, not evidence that it was right.

**Seventeen mutations; fifteen killed a named test.** Both survivors became **#0292** — the `try?` on
`SequencerSnapshot.capture` that #0286 added without a test, and the unparseable-file cleanup that
decision 24 has made nearly unreachable. Neither is a criterion violation.

**No earlier pin has gone stale**, including the one #0274 found rotted: `stillLive`'s mutation kills
`aStaleFileAttachesNothingAndIsRemoved` again.

**The concurrent-`around` race stays unfiled, and this pass agreed with the seventh's reasoning rather
than merely repeating it:** it is a TOCTOU against a directory git owns, no check inside `around` can
close it, and git defines nothing for two concurrent rebases in one worktree. It belongs to a
repository-locking issue. **One cheap mitigation is recorded with it** — an exclusive create
(`O_CREAT|O_EXCL`) would make first-writer-wins a filesystem property rather than a check-then-act one,
narrowing the window without pretending to close it.

**Clean-review count: 0.** It restarts when #0291 resolves.

## Ninth umbrella review — 2026-08-18. Not clean: one finding.

**Twenty-six full-suite mutations plus three probes; twenty-two killed a named test, three survived, one
was invalid.** The finding is **#0299**: `composing(with:)`'s `source` can take the *older* invocation's
value with the whole suite green, so a `switchyard fixup` against an existing commit stores its mapping
labelled `amend` when the authoritative invocation was the `rebase`. The composed mapping is the only
one a multi-invocation operation stores, so that is the label that survives.

**The review stated its own counter-argument** — no bullet names `source` for the own half — and left the
narrow reading visible rather than arguing past it. Filed as a finding because the fix is one assertion
on a test that already replays the invocations, and because #0255 set the precedent.

**One mutation was recorded as invalid rather than as a survivor**, which is the methodological catch of
the pass: substituting `Date(0)` for the timestamp collided with the exact value every pinning test
passes as its own `now:`. Re-run with a distinguishable value, the timestamp died immediately. That is
now preflight check **8h**.

**No earlier pin has gone stale**, including `stillLive` — the one #0274 caught rotting.

**Three proposals, none filed.** The observed-rewrite worktree `name` is unpinned on the linked-worktree
branch, but bullet 1 is met and the `path` half is pinned byte-exactly. Observed-entry ordering survives
being reversed — a within-one-millisecond monotonicity guarantee, and the same exposure exists on
`ref_updates`, which makes it #0153's listing surface rather than this umbrella's. And the
concurrent-`around` race stays unfiled for a **third** independent time — this pass enumerated the
sequential routes and closed each, including establishing that the restore route puts the repository
back into the operation the file names, so attaching there is correct rather than a misattach.

**Three carried items are now verifiably closed**: `Fixup.clearInFlightFile` is gone from the tree,
`RepositoryLayout`'s contents comment is accurate again, and decision 24 sits under `### Settled`.

**Clean-review count: 0.** It restarts when #0299 resolves.

## Tenth umbrella review — 2026-08-18. Not clean: one finding, and the pin it broke was a tautology.

**Twenty-eight mutations; twenty killed a named test, six survived, two were invalid.** The finding is
**#0303**: `attachingRewrite`'s carry-through — `command`, `label`, `guardRefs`, `agent`, `traversal` —
is unpinned, **each field individually**, and the test that reads as the pin cannot fail.

```swift
#expect(after == beforeAttach.attachingRewrite(mapping))
```

`attachRewrite` *builds* the stored value with that same function, so both sides of the `==` run the
same code on the same input. **Its comment even says "rather than through `attachRewrite` again"** —
describing something the line does not do, which is how this survived nine passes. Recorded as preflight
check **8j**.

**The review stated the honest limit of its own finding**: nothing can observe the defect today, because
no in-tree caller passes `command` or `agent` to `around`, no production writer populates `guardRefs`,
and `traversal` entries never receive an attach. It goes live the first time M3's CLI supplies those
fields to a `fixup`.

**Two invalid mutations, both recognised as such rather than reported as survivors** — one a no-op
precedence swap (`fromFile` is nil whenever `entryID` is not), one a check-8h collision (every
`extraInfo` in every composing test is `nil`). That is two passes running where the reviewer caught its
own bad mutation rather than filing it.

**Three carried proposals were measured rather than re-argued**, per check 8g: the composed mapping's
internal order (real behaviour change, but the non-empty `unconsumed` case is a **test-replay artifact**
— under the real hook the pairs chain and it is empty), the observed `worktree.name` on the
linked-worktree branch, and `JournalObserved.list`'s ordering, which belongs to #0153's shared listing
surface. **The concurrent-`around` race was declined a fourth time, and the review said plainly that it
did not measure it** and why — closing it needs a concurrency harness the suite does not have.

**No earlier pin has gone stale**, and **no sixth instance of the wrong-entry root cause**: decision 24
still holds under three separate mutations, including writing the file back beside the sequencer
directory.

**Clean-review count: 0.** It restarts when #0303 resolves.

## Eleventh umbrella review — 2026-08-18. Not clean: one finding, and a clean tautology hunt.

**Nineteen mutations; sixteen killed a named test, three survived, none invalid.** The finding is
**#0309**: `attachRewrite` reads the in-flight file **only** when the environment carried no id — so it
never clears a different, still-open operation's file — and nothing pinned that. Making the read
unconditional leaves entry *selection* correct and quietly clears another operation's live slot, so that
operation's own mapping later reaches nothing.

**The comment is the cause, and it is #0303's defect a second time.** Both the test's and production's
comments say the file is never opened because *"`??`'s right side is an autoclosure"* — untrue since the
`try` forced an explicit ternary. An assertion about the file's survival looked redundant *because the
comment said it could not be touched*.

**The tautology hunt the brief asked for came back empty**, and that is worth recording: a scripted scan
for `#expect(a == b)` with a shared call on both sides found nothing across five test files, and the
wire tests pin literal bytes with round-trips only as a documented complement. **#0303's own new
assertions were checked and are sound** — `beforeAttach` is deserialised from the repository rather than
rebuilt, so the two sides move independently — and its disclosed limit was confirmed rather than
overclaimed: `guardRefs` and `traversal` do survive being blanked, exactly as its test comment says.

**No earlier pin has gone stale.** The reviewer re-ran the #0274 question across **every** prior fix in
this umbrella — #0233, #0234, #0236/#0242, #0253/#0273, #0254, #0255, #0291, #0299, #0303 — and each
still kills the test written for it. **No sixth instance of the wrong-entry root cause.**

**The concurrent-`around` race was declined a fifth time**, with the reviewer noting plainly that it did
not measure it and why. It added one observation worth keeping: **this finding is a *sequential* version
of the same "whose file is this?" question**, which is mild evidence that the slot's **ownership model**
deserves one issue rather than continued case-by-case patching. Recorded for Brennan; not a criterion
violation.

**Clean-review count: 0.** It restarts when #0309 resolves.

## Twelfth umbrella review — 2026-08-18. Not clean: one finding, and an audit of every comment in the layer.

**Twelve mutations; eleven killed a named test, one survived, none invalid.** The finding is **#0317**:
`composing(with:)`'s `chainedFrom` bookkeeping is unpinned, and breaking it stores `headBefore` mapped to
**an intermediate commit no ref in the resulting history reaches** — exactly what #0233 and #0234 exist
to prevent.

**Why nine passes missed it is the useful part.** The complementary mutation — dropping `unconsumed`
entirely — *is* killed, but by a **test-replay artifact**: the test attaches all three logged
invocations, including the mid-rebase `amend` a live `isMidRebaseAmend` filters out. In the real
two-invocation sequence `unconsumed` is always empty, so that test constrains the "too few pairs"
direction and never observes "too many". The tenth pass recorded the *order* of
`composedRewrites + unconsumed` as a proposal; the *content* question is a different one.

**The comment audit the brief asked for was the pass's other product**, and it is worth reading rather
than repeating: roughly twenty doc comments across `JournalObserved.swift`, `JournalCheckpoint.swift`,
`JournalEntryMetadata.swift`, `PostRewrite.swift` and `RepositoryLayout.swift` were checked **against the
code** and found accurate — including #0309's rewritten one. **One inaccuracy was found and judged too
small to file** (`JournalEntryMetadata.swift:74-80` describes a caller convention as if it were a type
invariant), and one pair of statements about the same instant disagree harmlessly
(`JournalCheckpoint.swift:452` versus `:397-407`). After two passes where a wrong comment *was* the
defect, an explicit list of the ones that are right is what lets the next pass stop re-reading them.

**My brief was wrong about #0316 and the reviewer corrected it.** I said the new coverage scans would
reach this umbrella; they do not. `JournalObserved.Metadata.Kind` is a wire discriminator and
`PostRewrite.Source` is not `RawRepresentable`, so neither appears in `EnumVocabularyCoverageTests`. The
`JournalObservedWireTests` byte pins remain the coverage here, and they are adequate.

**No earlier pin has gone stale**, including the two newest — #0303's and #0309's were both re-verified
this pass.

**The concurrent-`around` race: declined a sixth time**, unmeasured, reasoning unchanged.

**Clean-review count: 0.** It restarts when #0317 resolves.
