# 0011 — Implement the JSON envelope, error shape, and exit code contract

| | |
|---|---|
| **Status** | wontfix |
| **Module** | yard |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-06 |

## Description

JSON is the contract; human-readable output is the courtesy. Agents break on inconsistent output
shapes far more often than on missing features, so the envelope is fixed once and versioned.

Every response carries `"schemaVersion": 1`. Failures in `--json` mode emit
`{"schemaVersion":1,"ok":false,"error":{"code":"…","message":"…","hint":"…"}}` **on stdout**, not a
bare string on stderr — an agent parsing stdout should never have to also parse stderr to find out
what went wrong.

## Expected behavior

- [ ] Envelope and error types live in `YardKit` and are used by every command.
- [ ] Exit codes match guide §6 exactly: 0 success, 1 usage, 2 broker unreachable, 3 app unavailable, 4 request failed, 5 session terminated, 6 repository error, 7 human declined, 8 blocked on conflicts, 9 signing failed.
- [ ] Codes 2–5 match RemoteControl and are not renumbered.
- [ ] Error codes are a closed enum, so an agent can branch on them.
- [ ] Tests assert the failure envelope goes to stdout and the exit code is set correctly.

## Notes

Guide §6 "Design principles" and "Exit codes". Blocked by #0006.

## Superseded by #0090, #0091, #0092, #0093

**Do not dispatch this issue.** It was split; the implementation work lives in #0090, #0091, #0092, #0093 and this file
remains only as the umbrella. The envelope type, the stdout/stderr discipline, the entry-point wiring, and the subprocess-level contract test are four deliverables. Same reason as #0010.

This closes when all of #0090, #0091, #0092, #0093 are resolved. Issues that list "Blocked by #0011" are in practice
blocked by those children, not by anything left here.

## Closed as superseded, 2026-08-07

All of #0090, #0091, #0092 and #0093 are resolved, and between them they deliver every criterion above. This issue was split
before dispatch for being oversized — the same shape that made #0022 fail — and nothing was ever built
under its own number.

Marked `wontfix` rather than `resolved` for that reason: the work exists, it just does not belong to
this issue. Leaving it `open` was overstating what M1 has left.
