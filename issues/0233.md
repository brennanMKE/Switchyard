# 0233 — The mid-rebase dedup rule is unreachable on the own-invocation path

| | |
|---|---|
| **Status** | resolved |
| **Module** | YardGit |
| **Milestone** | M2 |
| **Platform** | macOS |
| **First seen** | 2026-08-17 |

## Description

**Finding 1 of the #0160 umbrella review, 2026-08-17.** The mid-rebase dedup rule — *only the final
`rebase` invocation is authoritative* — is enforced on the **foreign** path and **structurally
unreachable on the own path**.

In `JournalObserved.record(_ decision:…)` the gate sits *after* the own/foreign split
(`JournalObserved.swift:142-149`):

```swift
guard !decision.isOwnInvocation else { return nil }      // own invocations leave here
if decision.source == .amend {                            // ...so never reach this
    let rebaseMergePath = try context.path(for: "rebase-merge", git: git)
    if fileManager.fileExists(atPath: rebaseMergePath) { return nil }
}
```

and `JournalCheckpoint.attachRewrite` (`JournalCheckpoint.swift:298-313`) has no equivalent.

**Established by the review**, in a scratch copy: deleting the whole `if decision.source == .amend`
block reddens exactly one test — `aMidRebaseAmendDoesNotProduceItsOwnObservedEntry` — and every
`PostRewriteAttach` test stays green. A probe that fired a real own autosquash and routed each
invocation through `attachRewrite` attached the mapping `50097fe → b122f62`, **whose old oid is an
intermediate commit that never existed before the rewrite**.

The tell was already in the test file: `PostRewriteAttachTests` picks the authoritative invocation
**itself**, in a helper (`finalInvocation` → `logged.last`). Nothing in production does that.

## The files

- **`YardKit/Sources/YardGit/JournalObserved.swift`** — the gate moves out of `record(_ decision:)`.
- **`YardKit/Sources/YardGit/JournalCheckpoint.swift`** — `attachRewrite` calls the shared gate.
- **`YardKit/Tests/YardGitTests/PostRewriteAttachTests.swift`** — the own-path test.
- **`YardKit/Tests/YardGitTests/JournalObservedTests.swift`** — the existing foreign test stays green.

Put the helper wherever both can reach it without a new file; `JournalObserved` already owns the
`rebase-merge` probe and is the natural home.

## Expected behavior

- [ ] The `rebase-merge` gate moves to a helper both `JournalObserved.record(_ decision:)` and
      `JournalCheckpoint.attachRewrite` call, **before** the own/foreign split — so the rule is one
      implementation rather than two, and cannot be enforced on one path only.
- [ ] An own-path test mirroring `aMidRebaseAmendDoesNotProduceItsOwnObservedEntry`: a mid-rebase
      `amend` decision with an entry id attaches **nothing**, and the entry's metadata still carries
      no `rewrite`.
- [ ] `PostRewriteAttachTests`' own `finalInvocation` helper is **no longer needed to make the tests
      pass** — the production code selects. Keep it if it still aids readability, but say whether a
      test now passes the mid-rebase invocation directly and gets nothing.
- [ ] Mutation, run and reported: delete the shared gate and confirm **both** the foreign test and the
      new own test redden.
- [ ] `cd YardKit && swift test` twice, both green, with **N greater than 1011**, the count on `main`
      measured 2026-08-17.

## Notes

Guide §6, #0043's Givens (the measured mid-rebase invocations). Parent **#0160**; sibling of **#0234**,
which is the other half of the same gap and should land after this one — with mid-rebase amends
filtered out, the composition #0234 has to handle is much smaller.

## Work log

- **Round 1 — 2026-08-17, Sonnet subagent, 101,048 tokens, 5m03s, accepted.**
  `JournalObserved.isMidRebaseAmend(_:in:git:fileManager:)` is the shared gate; `record(_ decision:)`
  and `attachRewrite` both call it **before** their own/foreign guard. `attachRewrite` gained a
  `fileManager:` parameter so the gate is injectable there too.
- **The round's mutation** (`isMidRebaseAmend` returning `false`) reddens both the foreign and the new
  own-path test, and leaves `aFinalRebaseInvocationIsRecordedEvenWhileRebaseMergeStillExists` green —
  which is the pair that proves the gate is shared *and* not over-skipping.
- **Mine was narrower and is the one that matters**: deleting the gate call from `attachRewrite`
  **only**. `aMidRebaseAmendAttachesNothingToItsInFlightEntry` reddens on both assertions while every
  foreign test stays green — so the own path is independently pinned, which is exactly what was
  missing before this issue.
- **`finalInvocation` in `PostRewriteAttachTests` is still used**, by
  `attachTargetsTheEntryNamedByIDNotTheNewestEntry`, to read the last entry off a real log. That is
  parsing, not filtering — the new test deliberately hands `attachRewrite` a mid-rebase invocation
  directly and asserts it gets nothing, so production does the selecting.
- **Review.** 1012 green twice with `main` merged in.

**Commit** — squashed to `main` from `issue/0233`. Suite 1011 → **1012**.
