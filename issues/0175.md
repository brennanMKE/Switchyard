# 0175 — Journal restore: cross-worktree application with per-worktree ref filtering

| | |
|---|---|
| **Status** | in-progress |
| **Module** | YardGit |
| **Milestone** | M2 |
| **Platform** | macOS |
| **First seen** | 2026-08-08 |


## Description

Cross-worktree restore: the `allowDifferentWorktree` override #0034 decision 5 promised, made
safe by the per-worktree ref filtering #0044 decision 4 requires. **Split from #0168's planning
pass on 2026-08-08** — #0168 delivers same-worktree restore only, its gate refuses every
cross-worktree restore, and its public `restore` signature deliberately carries no override
parameter: the parameter lands here, with the filtering that makes it safe, not before.

**Structured stub. Re-authored to code level at its own planning pass before dispatch, per
CLAUDE.md.** The binding design below is settled; the pass writes the code and measures it.

## The design — bound by #0044 decision 4 and #0168's decisions

- **Why verbatim application corrupts (measured, #0044):** `for-each-ref` lists the *capture
  worktree's* `refs/worktree/*` and `refs/rewritten/*` under plain names, so worktree A's
  snapshot applied verbatim from worktree B (a) writes A's private refs into **B's** namespace
  and (b) deletes B's own private refs as "extras" — `RefSnapshot.restore` plans deletions from
  the same listing rules as capture, which in B's context enumerates B's private refs.
- **The resolution is filter-and-preserve, not refuse.** #0044 offered either; refusal would
  make the recovery story impossible — a dead agent worktree's entry (recorded worktree pruned,
  #0168's gate says "no longer exists") restored from the main worktree is exactly the case the
  override exists for. Under the override, build the **applied snapshot** as: the recorded
  snapshot's entries whose names are NOT `WorktreeContext.isPerWorktree`, **plus the caller's
  current per-worktree entries** (from #0168 step 3's single capture — they land in the `wanted`
  set, so deletion planning leaves them alone and their updates are no-ops), with the recorded
  `HEAD` applying to the caller — documented, not hidden (#0034 decision 5).
- **The guard stays coherent under the override for free.** #0168 decision 1 made the guard's
  reference the *caller's own* scoped cursor entry — always recorded in the calling worktree —
  so no cross-worktree comparison ever happens; #0044 decision 3's incoherence cannot arise.
- **The gate honors the override in both liveness states**: a live recorded worktree and a
  pruned one both restore under `allowDifferentWorktree: true`; without it, #0168's typed
  refusals stand unchanged.
- **Disturbance and object checks run on the applied snapshot**, not the recorded one — the
  applied one is what `update-ref` will see.

## Expected behavior — sketch, superseded by the 2026-08-17 planning update below

- [ ] `YardKit/Sources/YardGit/JournalRestore.swift` gains `allowDifferentWorktree: Bool = false`
      and the applied-snapshot construction above (an **edit** — AGENTS.md Rule 5c).
- [ ] Cross-worktree round trip, both ref formats: an entry recorded in a linked worktree
      restores from the main worktree under the override — shared refs match the recorded
      snapshot, the caller's `HEAD` applies, and the caller's own `refs/worktree/*` survive
      untouched. **The fixture must actually create per-worktree refs on both sides**
      (`git update-ref refs/worktree/probe-<side> <oid>` in each worktree) and the pass must
      prove the survival assertions load-bearing by deleting those fixture lines and observing
      red (the emptiness rule in docs/review-failures.md).
- [ ] The recorded worktree's private refs are NOT written into the caller's namespace, and are
      not deleted from the recorded worktree's namespace either (they are simply outside a
      cross-worktree application's reach).
- [ ] A pruned recorded worktree restores under the override — the dead-agent recovery case.
- [ ] Without the override, #0168's gate tests still pass unchanged.
- [ ] `YardKit/Tests/YardGitTests/JournalRestoreTests.swift` gains the tests; the diff lists
      exactly two files; `swift test` prints a count line whose N must exceed the then-current
      baseline by the exact delta the planning pass states.
- [ ] A mutation table, every row applied and observed red with the killing test named — one row
      must drop the per-worktree filter (apply verbatim) and be killed by the
      namespace-survival test.

## Notes

- Blocked by #0168 (edits its files). Consumers: M3's `restore --worktree` surface; #0169 does
  NOT need this (undo/redo are same-worktree by construction of the scoped chain).
- The recorded `HEAD` may be symbolic to a branch that exists only as a shared ref — fine — or
  detached at an oid the object check (#0168 step 6) already covers.
- Engine + tests only: no `CommandSpec`, no `runYard` arm, no CLI wiring (M3).

## Planning update — 2026-08-17. The open question is settled; authored to code level.

**The design question this issue stopped on is now answered, and the answer is the one the design
section above already claimed.** The 2026-08-09 partial note said *"whether that reasoning still holds
across worktrees has not been established"*. It is established now, by reading the merged code rather
than by argument:

```swift
// JournalRestore.swift:271-274
if !bypassGuard,
   let cursor = try JournalWorktreeScope.state(
       of: nodes, in: context.worktreeName).cursor,
```

The guard's reference is the cursor of the scope **`in: context.worktreeName`** — the *calling*
worktree. By construction that entry was recorded by the caller, so both sides of
`CrossToolGuard.diff` come from the caller's own context whether or not the *target* entry belongs to
another worktree. **No cross-worktree comparison happens at any setting of the override**, so #0044
decision 3's incoherence cannot arise, exactly as the design section said. #0034 decision 4 was
corrected on 2026-08-17 to describe this cursor-as-reference rule, and the third umbrella review
verified it against the code.

Two consequences worth stating so the round does not re-derive them:

- **A nil cursor skips the guard entirely** — restoring from present, with nothing believed. Under the
  override that is the *common* case, since the caller often has no chain of its own for a foreign
  entry.
- **The guard is therefore not the thing protecting a cross-worktree restore.** `WorktreeDisturbance`
  is, and it already runs regardless of `bypassGuard` (#0203) and on the **applied** snapshot.

## Change 1 — `YardKit/Sources/YardGit/JournalRestore.swift`

The gate at line 242 gains the override. Note this is an **edit**, not a new file (AGENTS.md Rule 5c):

```swift
        // 2. The worktree gate — before everything else (#0044), unless the
        // caller opted into a cross-worktree application (#0175).
        guard metadata.worktree.name == context.worktreeName || allowDifferentWorktree else {
```

`restore` and `restoreAssumingLock` both take `allowDifferentWorktree: Bool = false`, threaded through
the same way `bypassGuard` already is — copy that parameter's path exactly rather than inventing a
second convention.

## Change 2 — the applied snapshot

After the gate and **before** the disturbance check, build what will actually be written. The recorded
snapshot is only correct for the worktree that captured it:

```swift
        // Under the override the recorded snapshot cannot be applied
        // verbatim: `for-each-ref` lists the CAPTURING worktree's
        // `refs/worktree/*` and `refs/rewritten/*` under plain names, so
        // applying it here would write that worktree's private refs into
        // ours and delete ours as extras (measured, #0044). Take the
        // recorded shared refs, and carry our own per-worktree entries
        // through unchanged so deletion planning leaves them alone.
        let applied: RefSnapshot = allowDifferentWorktree && metadata.worktree.name != context.worktreeName
            ? RefSnapshot(
                head: recorded.head,
                refs: recorded.withoutPerWorktreeRefs.refs
                    + current.refs.filter { WorktreeContext.isPerWorktree($0.name) })
            : recorded
```

`RefSnapshot.withoutPerWorktreeRefs` (`RefSnapshot.swift:87`) is #0175's already-merged half, and
`WorktreeContext.isPerWorktree` (`WorktreeContext.swift:170`) is the predicate. **Use both; do not write
a third copy of the namespace list.**

**Then use `applied` — not `recorded` — for the disturbance check, the missing-object check, and
`RefSnapshot.restore`.** The design section says why: the applied snapshot is what `update-ref` will
see. The pre-restore entry at step 7 still captures the *caller's* present, which is unchanged.

## Change 3 — tests in `YardKit/Tests/YardGitTests/JournalRestoreTests.swift`

Both parameterized over `FixtureRepository.RefFormat.supported()`, as that file's neighbours are:

1. `aForeignEntryRestoresUnderTheOverrideAndLeavesOurPrivateRefsAlone` — a linked worktree records an
   entry; the main worktree restores it with `allowDifferentWorktree: true`. Assert the shared refs
   match the recorded snapshot, the caller's `HEAD` applies, and the caller's own
   `refs/worktree/probe-main` **still exists with its original oid**. **The fixture must create
   `refs/worktree/probe-<side>` in both worktrees** (`git update-ref`), and the survival assertion must
   be shown load-bearing — delete the fixture's `probe-main` line, observe red, restore it, and report
   that.
2. `theRecordedWorktreesPrivateRefsAreNotWrittenIntoOurs` — after the same restore, assert
   `refs/worktree/probe-linked` does **not** exist in the caller's listing.
3. `withoutTheOverrideAForeignEntryStillRefuses` — the existing gate behaviour is unchanged at the
   default, naming both worktrees. This is the regression guard on #0168.

## Expected behavior

- [ ] `allowDifferentWorktree: Bool = false` on `restore` and `restoreAssumingLock`, threaded like
      `bypassGuard`.
- [ ] The applied-snapshot construction above, used by the disturbance check, the missing-object check
      and `RefSnapshot.restore`.
- [ ] The three tests above.
- [ ] `cd YardKit && swift test` prints a `Test run with N tests` line **greater than** the count in
      `docs/test-baseline.txt` by exactly **6** — three tests × two ref formats is six *cases* but
      **three** `@Test` declarations; count declarations, and state which number you expect before
      running.
- [ ] `WorktreeDisturbance` still refuses a sibling-disturbing restore **under the override** — it runs
      regardless of `bypassGuard` (#0203) and must not be weakened here.

## Mutation table

| # | mutation | expected |
|---|---|---|
| 1 | Apply `recorded` verbatim instead of `applied` | **RED** at both survival assertions — the caller's private refs are deleted as extras and the recorded worktree's are written into ours. This is the defect the whole issue exists for. |
| 2 | Filter the recorded per-worktree refs but **omit** carrying the caller's own through | **RED** at `…LeavesOurPrivateRefsAlone` only — deletion planning takes them as extras. This is the half that is easy to miss, and the reason the design section spells out "plus the caller's current per-worktree entries". |
| 3 | Make the gate ignore the override entirely | **RED** at tests 1 and 2, **GREEN** at test 3 — confirming test 3 pins the default rather than the override. |
| 4 | Run the disturbance check on `recorded` rather than `applied` | Report what you observe. The fixtures may not distinguish them; if so, say that rather than claiming a kill, and note that the ordering is argued from #0044 decision 3 rather than pinned. |

## Notes

**#0044's umbrella review is waiting on this issue** — it is the last unmet binding requirement, and
that umbrella closes when this and #0203 (done) are resolved.


---

## History — below this line is the record, not the spec

## Partial — 2026-08-09. The filter landed; the override did not.

**Done and merged:** `RefSnapshot.withoutPerWorktreeRefs`, plus the measurement it rests on.
`for-each-ref` lists `refs/worktree/*` and `refs/bisect/*` **only in the worktree that owns them**,
under plain names — verified against a real sibling worktree — so a snapshot is a per-worktree
artifact and applying one elsewhere would both plant those refs and delete the target's own as
"extras". Three mutations red: neutering the filter, a blunt `refs/` strip that would take every
branch with it, and forgetting `refs/rewritten`. 825 → 829.

**Not done: the `allowDifferentWorktree` parameter on `JournalRestore.restore`.** I stopped short
deliberately rather than half-landing it.

The override is not a parameter plus a filter call. #0044 decision 4 and #0168's ordering make it a
behavioural change to the restore flow:

- the worktree gate currently **throws** `differentWorktree` before anything else; under the
  override it must instead proceed and the `Report` must say what was skipped and why;
- `WorktreeDisturbance` must still run on the applied snapshot **regardless** of the override —
  that check answers a question the guard cannot, and bypassing it is how a sibling's checked-out
  branch gets clobbered silently (`update-ref` does that at exit 0);
- the cross-tool guard's reference snapshot is the **cursor's**, not the target's (#0168 decision
  1), and whether that reasoning still holds across worktrees has not been established.

That last point is the reason to stop: it is a design question, not typing. Doing it half-right
would produce a flag that looks safe and is not — and the whole point of this issue is that the
plumbing underneath it fails silently.

## Sequencing

Split from #0168's planning pass 2026-08-08, carrying #0044 decision 4's measured basis and
#0034 decision 5's override semantics. Re-authored to code level at its own planning pass.
