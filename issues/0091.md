# 0091 — Emit the failure envelope to stdout, and nothing else

| | |
|---|---|
| **Status** | open |
| **Module** | YardKit |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-06 |

## Description

`EnvelopeFail.write()` must put **exactly one thing** on stdout: the JSON envelope. Round 2 broke
this — a helper case named `.stderr` calls `Swift.print`, which writes to stdout, so a human-readable
`[error] <code>: <message>` line is emitted *before* the JSON. An agent parsing stdout gets a
non-JSON prefix line and fails on the first character.

This is the whole point of the issue: JSON is the contract, and the contract is that stdout contains
JSON and nothing else.

## Expected behavior

- [ ] `write()` emits the JSON envelope to stdout and **nothing else** — no prefix, no suffix, no human line.
- [ ] Anything written for human consumption goes to **actual stderr** via `FileHandle.standardError`, never `Swift.print`.
- [ ] Delete the `indirect` on `StandardStream` — the enum has no payload and does not need it.
- [ ] A test captures stdout and asserts the captured bytes parse as JSON with **no leading or trailing non-JSON content**.
- [ ] `swift build` succeeds and `swift test` prints a `Test run with N tests` line you paste.

## Notes

Split out of #0011. The file is `YardKit/Sources/YardKit/Envelope.swift`.

**Do not start from `issue/0011`.** That branch holds an earlier attempt that #0090 replaces. Start
from the `main` that contains #0090, and re-read the file before assuming the bug described above is
still present — it may already be gone, in which case this issue reduces to adding the test.

**`Swift.print` writes to stdout.** This is a verified fact, not something to check: a case named
`.stderr` that calls `Swift.print` still writes to stdout. Human-readable output must go through
`FileHandle.standardError` explicitly.

In-process capture is fine here; the subprocess test is #0093.

## Sequencing — blocked by #0090

**Blocked by #0090.** #0090 through #0093 all modify the same file,
`YardKit/Sources/YardKit/Envelope.swift`, so they are strictly sequential: each one branches from a
`main` that already contains its predecessor. Dispatching two of them at once produces two models
editing one file from a base that does not contain the other's work.

Branch this issue from `main` **after** #0090 is squash-merged, not before. A worktree created
early must be rebased onto that `main` first.

