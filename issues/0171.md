# 0171 — Full-capture undo: wire index and worktree snapshots into checkpoint and restore

| | |
|---|---|
| **Status** | open |
| **Module** | YardGit |
| **Milestone** | M2 |
| **Platform** | macOS |
| **First seen** | 2026-08-08 |

## Description

Full-capture undo: wire the index snapshot (#0151) and the worktree snapshot (#0152) into the
checkpoint flow (#0167) and the restore flow (#0168), flipping the `captured` flags from the
refs-only honesty of #0034 Decision 3 to the guide §11 decision 3 policy — capture everything
except ignored files, always. **This is the issue that makes the M2 exit criterion "undo
round-trips every mutating command, including with an unmerged index" claimable, and it unblocks
#0035.**

**Structured stub, split from #0034 on 2026-08-08.** Re-authored to code level at its own
planning pass before dispatch, per CLAUDE.md — and only after #0151 and #0152 exist, since it
composes their real APIs.

## The work — bound by #0034 (Decision 3)

Edits `YardKit/Sources/YardGit/JournalCheckpoint.swift` (#0167) and
`YardKit/Sources/YardGit/JournalRestore.swift` (#0168) — **no new production file**; AGENTS.md
Rule 5c applies (`edit`/`cat >>`, never `cat >` onto them):

- Capture: `IndexSnapshot` (#0151 — tree oid normally, raw index blob when unmerged) and
  `WorktreeSnapshot` (#0152 — stash-form commit, nil-able, plus untracked tree, nil-able) feed
  `JournalAnchor.Contents(indexTree:/indexBlob:/untrackedTree:)` (#0028's slots, already there)
  and extend `keepAlive` with the stash-form commit. Metadata `captured` reports what actually
  got captured, including the unmerged-index `index: "raw"` case (#0155's map).
- Restore: apply the pieces the entry captured, in an order the planning pass settles and
  measures (refs/HEAD, then index, then worktree; the unmerged index restored byte-for-byte per
  git internals §3's one sanctioned raw-file case). The `Report` gains the real pieces; the
  `.notCaptured` reasons disappear for new entries; **entries written before this issue still
  restore refs-only and still report honestly** — the compatibility case has a test.
- The #0151/#0152 stubs' assumptions consumed here, restated so drift is visible: `IndexSnapshot`
  capture returns a two-case enum (tree vs raw blob); `WorktreeSnapshot` capture returns two
  nil-able oids (stash-form commit, untracked tree) and never touches the stash stack. If either
  lands differently, this issue's planning pass reconciles before dispatch.

## Expected behavior (to be authored to code level)

- [ ] Checkpoint captures index + worktree + untracked; `captured` flips accordingly, including
      `index: "raw"` for an unmerged index (`FixtureRepository.conflicted()` — reuse it, per
      #0151's note).
- [ ] Round trip on a dirty repository, both ref formats: staged + unstaged + untracked +
      conflicted states each survive checkpoint → wreck → restore, asserted piecewise (refs,
      `ls-files --unmerged` byte-compare, worktree file bytes, untracked presence).
- [ ] A pre-#0171 entry (refs-only `captured`) still restores and still reports the omissions.
- [ ] Tests extend `YardKit/Tests/YardGitTests/JournalCheckpointTests.swift` /
      `YardKit/Tests/YardGitTests/JournalRestoreTests.swift` or add one new test file — the
      planning pass names it; the diff lists exactly the named files.
- [ ] `swift test` prints a `Test run with N tests` line; N must exceed the branch's pre-change
      count by the exact delta the planning pass states.
- [ ] A mutation table, every row applied and observed red with the killing test named — one row
      must drop the untracked tree from capture, killed by the untracked-survival test
      (internals §3's silent-data-loss warning, made a red test).

## Notes

- Blocked by #0151, #0152, #0167, #0168. Unblocks #0035 (the round-trip suite), which owns the
  exhaustive state matrix — this issue proves the wiring, #0035 proves every mutating command.
- Guide §11 decision 3 (capture everything except ignored files) is the policy this issue makes
  true; until it lands, #0034 Decision 3's refs-only honesty stands.
- Engine + tests only: no `CommandSpec`, no `runYard` arm, no CLI wiring (M3).

## Correction — 2026-08-08

**The wire value is `"raw"`, not `"blob"`.** This stub said `index: "blob"` (twice, now corrected).
The pinned wire vocabulary is `JournalEntryMetadata.IndexCapture`, whose values are
`false | "tree" | "raw"` — hand-written single-value `Codable` (#0155 decision 4), pinned by
golden-bytes tests because a bare `Codable` on a payload-free enum silently encodes `{"case":{}}`
under SE-0295.

Found by #0151's planning pass while mapping its API onto that vocabulary. A stub that names a wire
value the wire does not have is the kind of detail an implementer transcribes faithfully into a
defect.
