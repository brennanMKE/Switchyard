# 0334 — Decide whether gitStatus should ever report a copy record

| | |
|---|---|
| **Status** | resolved |
| **Module** | YardGit |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-18 |

## Description

**A consequence #0329 created, found by its own round and confirmed at review.** `gitStatus` now pins
`-c status.renames=true` so its payload cannot change shape under user config. A command-line `-c`
also beats a **repository-level `status.renames=copies`** — measured, git 2.50.1 — so **`gitStatus`
can no longer report a copy (`C`) record at all**, for any repository, however the user configures it.

**Correction, 2026-08-18, made when #0334's round pushed back:** this paragraph originally said
`WorktreeStatusEntry.State.copied` was a vocabulary element production can never emit. **There is no
`.copied` case.** `State.init(char:)` maps `"R", "C"` both to `.modified` (`WorktreeStatus.swift:69`),
as it always has. The real consequence is narrower and still worth the decision: a copy is reported as
an **uncorrelated `A`**, losing the `originalPath` correlation a `C` record would have carried — not a
dead enum case. The defect-class-1 framing was wrong.

The cost is visible in the diff. `doesNotThrowOnCopyDetectionRepository`
(`YardKit/Tests/YardGitTests/GitStatusTests.swift:165`) used to assert
`copy.originalPath == "original.txt"` and `copy.staged == .modified`; it now asserts
`originalPath == nil` and `staged == .added`. Its "confirm the config bites" probe still shows plain
`git status --porcelain=v2` emitting `C. N...` on that fixture — **git detects the copy, the engine
throws the information away.**

## Expected behavior — rewritten 2026-08-18 now that the decision is made

The decision is recorded below: **keep `status.renames=true`.** What remains is documenting the
consequence so nobody hunts for a code path that cannot run.

- [ ] `WorktreeStatusEntry.State.copied`'s doc comment says that **no production path emits it**:
      `gitStatus` pins `-c status.renames=true`, a command-line `-c` beats a repository-level
      `status.renames=copies`, and copy detection is therefore unreachable through the engine. Name
      **#0329** (the pin) and **#0334** (this decision).
- [ ] It also says what still covers the case: `EnumVocabularyCoverageTests`' hand-built `C.` record
      (#0316) keeps `State`'s parsing of `C` tested, so the case is dead in production but not
      untested.
- [ ] The comment block above `gitStatus`'s argument vector in `WorktreeStatus.swift` gains one
      sentence recording the same consequence, since that is where a reader meets the pin.
- [ ] Guide §11 gains the decision as a numbered entry under `### Settled`, with the three readings
      and why reading 1 won — the text is in this issue's `## Decided` section below; move it, do not
      re-derive it.
- [ ] **No production behaviour changes and no test changes.** If you find yourself editing
      `WorktreeStatus.swift`'s argument vector or any assertion, stop — that is out of scope and means
      the issue has been misread.
- [ ] `cd YardKit && swift test` **twice, in the foreground with an explicit 900000 ms timeout**, both
      green, at the count in `docs/test-baseline.txt` — **unchanged**, since this issue adds no tests.
      Say so explicitly rather than reporting a rise.

## The files

`YardKit/Sources/YardGit/WorktreeStatus.swift`, `YardKit/Tests/YardGitTests/GitStatusTests.swift`, and
`docs/switchyard-development-guide.md` §11.

## Notes

**Milestone M1**, criterion 5. Direct consequence of **#0329**; do not undo that pin — every reading
above keeps the payload config-blind, which is the property #0329 was for.

**Not a blocker for #0330.** The sweep test asserts the payload does not *vary* with config, and all
three readings satisfy that.

## Decided 2026-08-18 — reading 1, keep `status.renames=true`

**Decided by me, not by Brennan, because he asked to wrap M1 ASAP and this was the only thing in it
blocked on a person.** It is one line and reversible; if he wants copies, #0334 reopens.

**Why reading 1.** It is git's own default, so it changes nothing for anyone today; it keeps the
payload config-blind, which is what #0329 was for; and copy detection is the expensive `-C` pass git
leaves off by default, so pinning `copies` would slow every `status` call on a large repository to
surface something almost nothing consumes. Reading 3 — a `detectCopies:` parameter — stays available
the moment a caller actually wants it, and costs nothing to add later.

**What this issue still owes**, and it is small: `WorktreeStatusEntry.State.copied`'s doc comment must
say that **no production path can currently emit it**, that `gitStatus` pins `status.renames=true`, and
that `EnumVocabularyCoverageTests`' hand-built `C.` record is what keeps its parsing tested. Without
that note the next reader goes looking for the code path that produces it.

## Work log

- **Round 1 — 2026-08-18, Sonnet subagent, 93,017 tokens, 7m48s, accepted unchanged.** 1160 → 1160,
  unchanged, as a documentation-only issue should be.
- **The round corrected the issue and was right.** The checklist told it to document
  `WorktreeStatusEntry.State.copied`'s doc comment; **that case does not exist** — `"R", "C"` have both
  mapped to `.modified` all along. It documented the `"R", "C"` mapping line instead and said why.
  The Description above is corrected rather than left standing.
- **My second unverified assertion about the code today**, after #0341's claim that `HunkParser`
  cannot parse `diff-tree`'s oid line. Both are preflight 11 — *was every fact the issue asserts
  actually verified, by me, in this tree* — and both were caught by the round rather than by me.
  Recorded as a repeat rather than as a new check: the check exists and I skipped it.
- **Reviewed by re-running.** `main` merged in first; full suite twice on the merged result, both
  `Test run with 1160 tests in 81 suites passed`.
- **Commit** `HEAD` on `main`, squashed from `issue/0334`.
