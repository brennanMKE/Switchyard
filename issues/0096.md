# 0096 — Implement switchyard wt gc

| | |
|---|---|
| **Status** | open |
| **Module** | yard / YardGit |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-06 |

## Description

Wrap `git worktree prune` and additionally report what is prunable and which worktrees hold an
abandoned agent session — the mess left behind when an agent crashes or a container is torn down
mid-run.

## Givens — verified on `main`, treat as true

1. `YardKit/Sources/YardGit/WorktreeContext.swift` provides `WorktreeContext.resolve(...)` plus
   `topLevel: String?` (nil in a bare repo), `gitDir`, `commonDir`, `worktreeName: String?`,
   `isLinkedWorktree`, `isBare`, and `path(for:git:)`. Use it; do not resolve paths yourself.
2. **`WorktreeContext` does not expose the main worktree's path.** If you need it, take the **first**
   `worktree <path>` line of `git worktree list --porcelain` — that entry is the main worktree from
   any worktree in the repo. Verified from inside a linked worktree. Do not derive it by trimming
   `/.git` off `commonDir`; that breaks on bare repos and on a `.git` file pointing elsewhere.
3. `YardKit/Sources/YardGit/FixtureRepository.swift` provides — verified by reading it, these are
   the real signatures:

   ```swift
   public func addWorktree(named name: String, branch: String) throws -> URL
   public func addWorktree(path targetDir: String, branch: String) throws -> URL
   public func lockWorktree(_ path: URL, reason: String) throws     // ← you need this one
   public mutating func build(_ commits: [Commit]) throws
   public mutating func branch(_ name: String, at commit: String? = nil) throws
   public func destroy()
   ```

   **`lockWorktree(_:reason:)` already exists.** Do not shell out to `git worktree lock` in a test
   and do not add a second helper. Tests build a **real** linked worktree, not a simulated one.
   `repo.oids` is populated only by `build(_:)` — force-unwrapping it for anything else traps and
   aborts the whole suite, which cost #0112 a round.
4. **Every shell-out goes through `GitProcess`** (`YardKit/Sources/YardGit/GitProcess.swift`). Do not
   construct `Process` directly — the boundary is centralised there so it stays testable, and
   libgit2 does not run hooks.
5. **Never build a path by concatenating onto `.git/`.** Resolve through `git rev-parse --git-path`
   or `WorktreeContext`. Reftable repositories have no `.git/refs/**` at all and return nothing
   rather than erroring.

## Tests must be able to fail

- Assert any extraction is non-empty before comparing it. A helper that silently returns nothing
  turns the next assertion into `[] == []`, which passes unconditionally — that cost #0087 a round.
- No `#expect(true)` or any assertion whose operands are literals.
- Exercise the real failure path, not only the happy one. Say how you confirmed the test fails when
  the behaviour is wrong, rather than asserting that it does.

**Run these four mutations and report each result individually.** A green suite is not evidence —
every rejected round in this project had a green suite.

| # | mutation | required |
|---|---|---|
| 1 | parse `Output.text` instead of `Output.standardError` for the prune output | the prune test FAILS |
| 2 | make `--prune` the default (always prune) | the report-only test FAILS — a prunable entry must survive a default run |
| 3 | treat every `locked` entry as an abandoned session, ignoring whether the directory exists | the test with a locked-and-present worktree FAILS |
| 4 | drop the `switchyard-agent:` prefix check | a user-locked worktree is misreported as abandoned and that test FAILS |

Restore after each and confirm the suite is green and `git diff` is unchanged.

The fixture for mutations 3 and 4 needs **three** locked worktrees: one locked by an agent and
present, one locked by an agent and deleted, one locked by the user with an unrelated reason and
deleted. Only the middle one is an abandoned session. A one-worktree test passes for almost every
wrong implementation.


## Probed 2026-08-07 against real repositories — these supersede the 2026-08-06 notes

**1. `git worktree list --porcelain` already reports prunability. There is no discovery command to
write.** A worktree whose directory is gone gains a fourth line:

```
worktree /…/w1
HEAD f16ed35…
branch refs/heads/w1
prunable gitdir file points to non-existent location
```

The reason after `prunable ` is git's own wording — report it verbatim, do not paraphrase it.

**2. A *locked* worktree whose directory is gone is never reported `prunable`, and `prune` never
touches it.** It keeps its `locked <reason>` line and nothing else changes:

```
worktree /…/w2
HEAD f16ed35…
branch refs/heads/w2
locked switchyard-agent:session=abc123
```

Verified: deleted both `w1` (unlocked) and `w2` (locked), ran `git worktree prune -v`, and only `w1`
was reaped. **So "never removes a worktree still locked by a live agent session" is git's guarantee,
not code you write.** Do not reimplement it. What you *do* write is the report: an **abandoned
session** is an entry that has a `locked` line whose reason begins `switchyard-agent:` **and** whose
worktree directory does not exist on disk. Git will never clean these up on its own — that is the
mess this command exists to surface.

Checking the worktree directory's existence with `FileManager` is correct here: it is a worktree
path, not a `$GIT_DIR` path. Report it; **never remove it**. A missing directory can also mean an
unmounted volume, and the whole point of a lock is that something else claims the entry.

**3. `switchyard-agent:` is the lock-reason prefix.** #0021 will write it; it is settled here so this
issue does not have to wait. Anything else in a `locked` reason belongs to the user and is reported
as a plain lock, not an abandoned session.

**4. `--dry-run -v` produces byte-identical output to the real `-v` run.** Both print one line per
reaped entry:

```
Removing worktrees/w1: gitdir file points to non-existent location
```

**5. Both `prune -v` and `repair` write to stderr, not stdout.** Verified by redirecting each stream
separately: `git worktree prune --dry-run -v 2>/dev/null` prints **nothing**.
`GitProcess.Output.text` and `.lines` are **stdout only** — parsing them gives an empty result and a
round that reports "nothing to prune" for every repository. Use `Output.standardError`.

## The safety finding that shapes this command

**`git worktree prune` cannot tell a *moved* worktree from a *deleted* one, and reaping a moved one is
not recoverable.** Verified end to end:

1. `git worktree add ../w1`, then `mv ../w1 ../w1-moved`.
2. `git worktree list --porcelain` reports the **old** path as
   `prunable gitdir file points to non-existent location` — exactly what a deleted worktree reports.
3. `git worktree prune` reaps it. The directory is **still on disk**, full of the user's work.
4. `git worktree repair ../w1-moved` now fails, exit 1:
   `error: unable to locate repository; .git file does not reference a repository`.

Before the prune, that same repair would have succeeded. Pruning is what makes it unrecoverable.

**Therefore: reporting is the default and pruning is opt-in.** `switchyard wt gc` with no flag
reports and changes nothing. Actually pruning requires an explicit flag — name it `--prune`. Do not
invert this into `--dry-run` on a destructive default; the whole finding above is why.

### Verified `git worktree` behaviour — probed 2026-08-06, still true

Run against a real repo with a linked worktree, not read from the man page.

- **`git worktree remove <path>` refuses a dirty worktree with exit `128`** and the message
  `fatal: '<path>' contains modified or untracked files, use --force to delete it`.
  `--force` then succeeds. "Dirty" includes untracked files, not just modifications.
- **`git worktree prune -v`** prints one line per reaped entry:
  `Removing worktrees/<name>: gitdir file points to non-existent location`, and the entry disappears
  from `git worktree list --porcelain`.
- **`git worktree repair <path>`** prints `repair: gitdir incorrect: <path-to-admin-gitdir-file>`.

**The counter-intuitive one, and the reason it is written down:** after a linked worktree is *moved*
on disk, `git -C <new-path> status` **still exits 0**. The worktree keeps working from the inside,
because its own `.git` file still points at the administrative directory. What breaks is the
*reverse* pointer — the `worktrees/<name>/gitdir` file in the main repository still names the old
location. So a test asserting "status fails before repair" would be **wrong**, and would pass or fail
for reasons unrelated to the code under test. Assert on the administrative state, or on an operation
run from the main repository, not on `status` from inside the moved worktree.

## Expected behavior

- [ ] New file `YardKit/Sources/YardGit/WorktreePrune.swift`. Create it; do not restructure existing files.
- [ ] Parses `git worktree list --porcelain` for `prunable` and `locked` lines. Reports each prunable
      worktree with **git's own reason string, verbatim**.
- [ ] Reports abandoned sessions: `locked` with a reason beginning `switchyard-agent:`, and the
      worktree directory absent from disk. Reported, **never removed**.
- [ ] **Reporting is the default.** Pruning happens only under an explicit `--prune`. See the safety
      finding above — this is not a stylistic preference.
- [ ] Reads pruning output from `Output.standardError`, not `.text` / `.lines`.
- [ ] Tests use **swift-testing** — `import Testing`, `@Test`, `#expect`. Not XCTest. `swift test`'s summary line counts only swift-testing tests, so an XCTest file passes invisibly.
- [ ] New tests go in **`YardKit/Tests/YardGitTests/`**. `Tests/YardGit/` is not a declared target
      path; a file there is silently never compiled, the suite stays green, and the count does not
      move. That cost #0095 a round.
- [ ] `cd YardKit && swift build` succeeds, and `swift test` prints a `Test run with N tests` line
      you paste. **`main` reported 225 on 2026-08-07** (216 was stale — #0095, #0110 and #0112 have merged since). Run `swift test` on the branch before your
      change for its own baseline, paste that too, and N must exceed it.

## Notes

Split out of #0022. Blocked by #0009, #0024.

The live-session guard was originally the criterion that mattered. The 2026-08-07 probe showed git
already provides it — a locked worktree is never `prunable` and never reaped. **What is left for this
command is the report**, plus the opt-in gate that keeps a moved worktree from being orphaned.

No longer blocked by #0021: the `switchyard-agent:` lock-reason prefix is settled above, and
`FixtureRepository.lockWorktree` already exists, so the fixtures need nothing that is not on `main`.

## Review round 1 — the engine is right; there are no tests, and no command

Exit 0 in 442s, and **4,251 output tokens**, which is the whole story: it wrote one file and stopped.
The log ends with *"Would you like me to proceed with step 1 (write tests) and the rest of the
verification workflow?"* after the model listed its own four remaining steps. It knew what was left
and asked instead of doing it.

**`WorktreePrune.swift` itself is good.** It reads `Output.standardError` — the defect this issue
warned about hardest — and against a real fixture with two linked worktrees whose directories were
both deleted:

```
report(from:)  → 2 entries
   locked   → .abandonedSession  removable=false  lockReason=switchyard-agent:session=x
   unlocked → .prunable          reason="gitdir file points to non-existent location"  (git's verbatim wording)
runPrune       → ["Removing worktrees/wt-unlocked: gitdir file points to non-existent location", ""]
   the locked entry survived
```

Keep the file. Round 2 needs three things.

### 1. There are no tests at all

`grep -rln 'WorktreePrune' YardKit/Tests/` returns nothing, so **all four mutations are unrunnable**
— each leaves the suite green at 225 by construction. The pre-change baseline measured by deleting
the new file is also 225. Nothing this round produced is verified by anything.

### 2. There is no entry point, so "reporting is the default" has nothing to attach to

`report(from:git:)` and `runPrune(repositoryPath:git:)` are two unrelated statics and nothing chooses
between them. That is the issue's fault for naming only a file. **The signature is settled here:**

```swift
public static func gc(
    repositoryPath: String,
    prune: Bool = false,
    git: GitProcess = GitProcess()
) throws -> (reports: [Report], pruned: [String])
```

`prune` defaults to `false`. When it is `false`, `pruned` is empty and **no `git worktree prune` runs
at all** — not a prune whose output is discarded. Keep `report` and `runPrune` as the pieces `gc`
calls; do not delete them.

### 3. `runPrune` returns a trailing empty string

`["Removing worktrees/…", ""]` — `omittingEmptySubsequences: false` on the final newline. Drop empty
lines.

### Verification for round 2 — do not stop to ask

Write the tests, run them, run the mutations, and report. **A round that ends with a question has
failed**; if something is genuinely undecidable, do the rest first and say so at the end.

- [ ] The fixture has **three** locked worktrees — agent-locked and present, agent-locked and deleted,
      user-locked with an unrelated reason and deleted — plus one unlocked and deleted. Only the
      second is an abandoned session. Build the locks with
      `FixtureRepository.lockWorktree(_:reason:)`, which already exists.
- [ ] All four mutations run and fail, each reported separately: (1) `.standardError` → `.text`;
      (2) `prune` defaulting to `true`; (3) ignoring directory existence for `locked`; (4) dropping the
      `switchyard-agent:` prefix check.
- [ ] A default `gc(repositoryPath:)` call leaves the prunable entry still listed afterwards. Assert
      on a second `worktreeList` call, not on the return value alone.
- [ ] `swift test` prints `Test run with N tests` with **N > 225**.

## Review round 2 — the test file does not compile. Round 3 is the last one.

```
WorktreePruneGcTests.swift:63:13: error: cannot find 'fm' in scope
```

`fm` is declared inside `gcDefaultDoesNotRunPrune` and used inside `gcWithPruneFlagCallsBothSteps`.
There is **no `Test run with N tests` line at all**, so all four mutations are unrunnable — round 1's
outcome reached a different way. The branch baseline with the file moved aside is 225; with it, the
suite does not build.

**The round was misled by `swift build`.** It ran `swift build`, got `Build complete!` in 0.08s, and
treated the suite as green. `swift build` does not compile test targets. Then `swift test | grep -E
"Test run|passed|failed"` returned nothing, and it read the empty output as inconclusive rather than
fatal — its own words three lines above `error: fatalError` were *"just warnings — no errors"*.

**The engine is correct and needs no further work.** Verified outside the broken suite, through a
throwaway package against a real four-worktree fixture:

```
DEFAULT gc -> reports=2 pruned=0
  abandonedSession removable=false lock=switchyard-agent:session=gone   path=…/agentdeleted
  prunable         removable=true  reason=gitdir file points to non-existent location  path=…/unlocked
gc(prune: true) -> ["Removing worktrees/unlocked: gitdir file points to non-existent location"]
```

`agentpresent` correctly absent, `userdeleted` correctly not abandoned, the prunable entry still
listed after a default `gc`, no trailing empty string, and every locked entry surviving the prune.
`gc(repositoryPath:prune:git:)` exists with `prune: Bool = false` and the default branch never calls
`runPrune`. **Do not touch `WorktreePrune.swift`.** `git diff -- YardKit/Sources/` should be empty at
the end of round 3.

**Round 3 is the test file and nothing else.** Four things:

### 1. Fix the compile error

`fm` must be in scope where it is used. Then **run `swift test`, not `swift build`** — and if the
output has no `Test run with N tests` line, the round has failed, whatever else it prints.

### 2. Stop moving `.git/worktrees` — delete the worktree directory instead

Seven `FileManager.moveItem` calls on `"\(repo.url.path)/.git/worktrees"` — lines 30, 32, 63, 97,
128, 159, 346. That is a raw `.git/` path concatenation, forbidden by CLAUDE.md and by Given 5 of this
issue, and it is not what the fixture needs.

**To make a worktree prunable, delete the worktree's own directory** — the path `addWorktree` returned:

```swift
let wt = try repo.addWorktree(named: "unlocked", branch: "b1")
try FileManager.default.removeItem(at: wt)     // now `git worktree list` reports it prunable
```

That is what the issue's probe did and what the reviewer's fixture confirmed produces
`prunable gitdir file points to non-existent location`.

### 3. Every test needs the full four-worktree fixture

`mutation3_…` builds only two, and leaves `userDeletedPath` on disk — so its "user-locked and
deleted" case is actually user-locked and **present**, and the test cannot distinguish the two things
mutation 3 is about. Build one helper that returns all four, and use it everywhere:

| worktree | lock reason | directory |
|---|---|---|
| `agentpresent` | `switchyard-agent:session=live` | **present** |
| `agentdeleted` | `switchyard-agent:session=gone` | deleted |
| `userdeleted` | `reviewing this branch later` | deleted |
| `unlocked` | — | deleted |

Only `agentdeleted` is an abandoned session. There is **no `mutation2_` test at all** — only
`mutation1_`, `mutation3_` and `mutation4_` exist. Add it: a default `gc` must leave the prunable
entry listed, asserted by a second `worktreeList` call.

### 4. Every assertion must be unconditional

These all pass when the thing they are about is absent:

```swift
#expect(!result.pruned.isEmpty || result.reports.contains { $0.type == .prunable })   // disjunction
if let presentLocked { #expect(...) } else { /* comment only */ }                     // skipped
if let e = …, e.type == .abandonedSession { Issue.record(…) }                         // never fires
_ = result.pruned                                                                     // asserts nothing
```

**Bind with `try #require(...)` and then assert.** `#require` fails the test when the value is absent,
which is the whole point — an `if let` around an assertion turns a missing entry into a pass.

### Verification for round 3

- [ ] `swift test` prints `Test run with N tests` with **N > 225**. No line at all is a failure.
- [ ] `grep -c '\.git/' YardKit/Tests/YardGitTests/WorktreePruneGcTests.swift` returns **0**.
- [ ] `grep -c 'if let' `on the test file: every remaining one is followed by an assertion in **both**
      branches, or is a `try #require`. No `else` block containing only a comment.
- [ ] All four mutations run and fail, each reported separately, including **mutation 2** — `prune`
      defaulting to `true`.
- [ ] `git diff -- YardKit/Sources/` is **empty**.

## Review round 3 — the cap is spent. Superseded by #0120.

Killed at 1800s with **SIGTERM**. The suite compiles and then **crashes**: no `Test run with N tests`
line, exit 1, `Fatal error: Unexpectedly found nil while unwrapping an Optional value`.

Round 3 fixed the one thing it was asked to fix first — `grep -c '\.git/'` on the test file returns
**0**, and the fixture now deletes worktree directories rather than moving `.git/worktrees`. Every
other criterion failed, and all of them from a single omission.

### One missing `removeItem` caused all four baseline failures

`buildFourWorktrees` creates `agentpresent`, `agentdeleted`, `userdeleted` and `unlocked`, locks the
first three, and then deletes **only `agentdeleted` and `userdeleted` — both locked**. Its own doc
comment says `unlocked` is deleted; the code never does it.

**Git does not mark a locked worktree prunable.** So zero prunable entries existed and every
`.prunable` assertion failed on the *unmutated* tree. The round's own debug output said so —
`prunable=false` on all five entries — and it spent the remaining twenty minutes adding more logging
rather than reading it.

**This issue never said why the fourth worktree had to be the deleted unlocked one.** It gave the
table and not the mechanism, so an omission in the fixture looked like an engine bug. That is a
`spec-defect`, and it is mine.

### The crash: `#expect(x != nil)` then `x!`

At `:215-217`, `:240-242`, `:294-296`. `#expect` records an issue and **keeps going**, so the force
unwrap traps, the process dies, and swift-testing emits no summary at all — the whole suite's result
is lost, not just that test's. `try #require` is the construct that stops.

Underneath that, a second modelling error: the tests look up `agentpresent` in `reports`. `gc` reports
only `.prunable` and `.abandonedSession` entries, so a present, agent-locked worktree is **correctly
absent** — it exists in the fixture so a test can assert its absence.

### Scope violation

Ten `print("[GC] …")` lines added to `WorktreePrune.report`, in an engine the issue said was correct
and must not change. That pollutes stdout for a CLI whose contract is JSON on stdout. Reverted before
the round was committed.

### Disposition

Three rounds spent, cap exhausted, status back to `open`. **The engine is correct and stays on
`issue/0096`.** The remaining work — the test file, with the mechanism spelled out — is **#0120**,
which branches from `issue/0096` so the engine comes with it. When #0120 lands, both issues close at
that commit.
