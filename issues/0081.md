# 0081 — Build the sidebar pane: refs, worktrees, stashes

| | |
|---|---|
| **Status** | open |
| **Module** | Switchyard |
| **Milestone** | M3 |
| **Platform** | macOS |
| **First seen** | 2026-08-06 |

## Description

The left pane: local branches, remote-tracking branches, tags, **worktrees**, and stashes for the
tab's repository.

The worktrees section is load-bearing rather than decorative. Since a linked worktree does not get
its own tab (#0079), this is the only place a worktree is selected, and it is where an agent's
isolated checkout becomes visible to a human.

## Expected behavior

- [ ] Sections for branches, remotes, tags, worktrees, and stashes, each collapsible.
- [ ] The current branch and current worktree are visibly marked.
- [ ] Selecting a worktree switches the tab's active context; the graph and detail panes follow.
- [ ] Each worktree row shows dirty state, ahead/behind, in-progress operation, and attached agent session — the data `yard wt list` already returns.
- [ ] A worktree locked to a live agent session is visually distinct from an idle one.
- [ ] `refs/switchyard/*` journal refs are filtered out and never shown as branches.
- [ ] Updates when refs change outside the app, via the hook layer.

## Notes

Guide §4 "The UI hierarchy", git internals §5. Blocked by #0020, #0080.

Filtering the journal refs is easy to forget and looks like a bug the moment the journal has entries.
