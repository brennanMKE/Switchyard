# 0094 — Implement yard wt where

| | |
|---|---|
| **Status** | open |
| **Module** | yard / YardGit |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-06 |

## Description

Resolve and report the current worktree context: worktree id, path, `$GIT_DIR`, `$GIT_COMMON_DIR`,
and the main worktree's path. The smallest of the worktree commands and a good first one — it is a
pure read over `WorktreeContext`, which #0009 already built.

## Expected behavior

- [ ] New file `YardKit/Sources/YardGit/WorktreeWhere.swift`. Create it; do not restructure existing files.
- [ ] A function taking a path and returning a value type with those five fields — no printing.
- [ ] Correct in the main worktree, in a linked worktree, and in a bare repository.
- [ ] Uses `WorktreeContext` (#0009) rather than resolving paths itself.
- [ ] Tests build a real linked worktree via `FixtureRepository` (#0024) and assert `$GIT_DIR` differs from `$GIT_COMMON_DIR` there and matches in the main worktree.
- [ ] `swift build` succeeds and `swift test` prints a `Test run with N tests` line you paste.

## Notes

Split out of #0022, which asked for four commands at once. Blocked by #0009 and #0024, both resolved.

Return a value; do not print. The CLI wiring is a separate concern.
