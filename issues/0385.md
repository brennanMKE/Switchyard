# 0385 — Spike: does an alert's disabled button update as the user types

| | |
|---|---|
| **Status** | resolved |
| **Module** | YardUI |
| **Milestone** | M7 |
| **Platform** | macOS |
| **First seen** | 2026-09-12 |

## Description

**Child of #0380.** **Question:** in `.alert(_:isPresented:actions:message:)` containing a `TextField`,
do a `Button`'s `.disabled(…)` state and the message `Text` update live as the user types?

**Why it matters:** #0363 uses an alert for New Branch… and Rename…, with the confirming button
disabled while `RefNameProblem` reports a problem.

### Build

Tab **0385 alert** of #0380's spike app.

### Observe

Open **New Branch…** and type `m`, `ma`, `mai`, `main`, `main2`. After each, record whether
**Create Branch** is enabled and what the message reads. Then clear the field.

The live behaviour to look for: disabled while empty and at `main`, enabled otherwise; the message
reads "A branch named “main” already exists" exactly while the field holds `main`.

### Pass or fail, and what changes

- **Pass** — both the button and the message update live. #0363 is unchanged.
- **Partial** — the button updates but the message stays frozen. #0363 cannot show the reason in an
  alert, so both dialogs move to sheets, as in the fail case.
- **Fail** — neither updates. #0363's New Branch… and Rename… become sheets: a `TextField`, the
  `RefNameProblem` text below it in `.secondary`, the confirming button as `.defaultAction`, and
  Cancel as `.cancelAction`.

## Expected behavior

- [ ] The observation is recorded here: versions, and the button state and message after each
      keystroke.
- [ ] #0363 is updated per the outcome.

## Notes

Parent: #0380. Guidance affected: #0363.

## Blocked on human observation — 2026-09-22

This spike needs a person at the Mac: typing into a presented alert and watching the button and
message update cannot be synthesized by an unattended round (#0380 §Who runs it). Brennan
instructed autonomous operation on 2026-09-22 ("work autonomously like I am AFK"), so the
observation is deferred, not answered. **Unavoidably blocked on human input for the M7 quest.**

**Unblock:** relaunch the spike app (`cd build/spike-m7 && swift build && .build/debug/M7Spikes`),
tab **0385 alert**, click New Branch…, type, record pass/fail here.
macOS 27.0, Xcode 27.0 (27A266a) at time of blocking.

## Observation — 2026-09-22 (core question PASS, one gap filed, via VM UI test)

Re-derived as `SwitchyardUITests/0385AlertLiveUpdateUITests.swift` against the real Create Branch
sheet in a fresh VM clone — macOS 26.6.2, Xcode 27.0 — run twice; both runs identical.

- The button's enabled state **updates live as the user types**: disabled when empty, enabled
  after one keystroke. That is the spike's question, and it passes.
- **Gap found**: the button **stays enabled on a colliding branch name** — no `RefNameProblem`
  check exists in the sheet. The spike's collision assertion fails, and that is the recorded
  answer: adding the collision refusal is product work, filed as #0397 for #0363's surface.
- The alert/message half of the original question is unobservable: the real surface is a sheet
  with no problem text. Recorded as unobservable, not as a pass.
