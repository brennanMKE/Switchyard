# 0010 — Declare command metadata as data, driving help, schemas, and the skill

| | |
|---|---|
| **Status** | in-progress |
| **Module** | yard |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-06 |

## Description

Command names, flags, exit codes, and response schemas are declared in one place as data, not spread
across `switch` statements. Three consumers derive from it: `--help`, the JSON schema output, and the
generated agent skill (#0068).

Deciding this late means retrofitting every command, and a hand-maintained skill drifts from the
binary within a milestone. A skill that lies about flags is worse than no skill.

## Expected behavior

- [ ] Every command declares its name, summary, flags, exit codes, and response schema in one structure.
- [ ] `yard --help` and `yard <cmd> --help` render from it.
- [ ] `yard schema [<command>] --json` emits the response schemas from the same source.
- [ ] A test asserts every registered command has a schema and at least one documented exit code.
- [ ] Adding a command with no documentation fails the build or a test, not review.

## Notes

Guide §6 "Design principles". Blocked by #0006. Blocks #0066.

## Review — round 1 rejected (timed out), and the issue is being split

Round 1 hit the 2400s watchdog mid-edit and left a **broken build**: 23 compile errors
(`cannot find 'Atomic' in scope`, `type 'String' has no member 'opt'`,
`cannot find 'usageSuffix' in scope`), so `swift test` never executed. It also regressed
`main.swift` — dropped `import YardGit` / `import YardKit` and the version print, and added an
`args.first!` force-unwrap *before* the empty-args guard.

**The diagnosis is not that the model failed. It is that this issue is too large to delegate as one
round.** It asks for a metadata model, a help renderer, a schema emitter, and a test, all at once,
with no existing pattern in the codebase to follow. Four delegated rounds have now been attempted
across three issues and the two large ones (#0010, #0011) both failed while the small documentation
one nearly succeeded on its first try.

**This issue is superseded by a split.** Do not re-dispatch it as written. See #0085, #0086, #0087,
#0088 — each is one file with one clear shape and a test that can settle it.

Round 1's code is committed on this branch as an artifact; it is not a foundation to build on.
