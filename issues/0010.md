# 0010 — Declare command metadata as data, driving help, schemas, and the skill

| | |
|---|---|
| **Status** | wontfix |
| **Module** | yard |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-06 |

## Description

Command names, flags, exit codes, and response schemas are declared in one place as data, not spread
across `switch` statements. Three consumers derive from it: `--help`, the JSON schema output, and the
generated agent skill (#0068).

Deciding this late means retrofitting every command, and a hand-maintained skill drifts from the
binary within a milestone. A skill that lies about flags is worse than no skill.

## Expected behavior

- [ ] Every command declares its name, summary, flags, exit codes, and response schema in one structure.
- [ ] `yard --help` and `yard <cmd> --help` render from it.
- [ ] `yard schema [<command>] --json` emits the response schemas from the same source.
- [ ] A test asserts every registered command has a schema and at least one documented exit code.
- [ ] Adding a command with no documentation fails the build or a test, not review.

## Notes

Guide §6 "Design principles". Blocked by #0006. Blocks #0066.

## Superseded by #0085, #0086, #0087, #0088

**Do not dispatch this issue.** It was split; the implementation work lives in #0085, #0086, #0087, #0088 and this file
remains only as the umbrella. Declaring the metadata, rendering `--help` from it, emitting schemas from it, and wiring the registry are four deliverables, and issues with four deliverables have not converged in this project (see docs/local-ai-workflow-log.md 3.6).

This closes when all of #0085, #0086, #0087, #0088 are resolved. Issues that list "Blocked by #0010" are in practice
blocked by those children, not by anything left here.

## Closed as superseded, 2026-08-07

All of #0085, #0086, #0087 and #0088 are resolved, and between them they deliver every criterion above. This issue was split
before dispatch for being oversized — the same shape that made #0022 fail — and nothing was ever built
under its own number.

Marked `wontfix` rather than `resolved` for that reason: the work exists, it just does not belong to
this issue. Leaving it `open` was overstating what M1 has left.
