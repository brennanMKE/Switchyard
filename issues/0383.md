# 0383 — Spike: do ⌥⌘↑ and ⌥⌘↓ reach the menu bar while the History list has focus

| | |
|---|---|
| **Status** | open |
| **Module** | YardUI |
| **Milestone** | M7 |
| **Platform** | macOS |
| **First seen** | 2026-09-12 |

## Description

**Child of #0380.** **Question:** with the History `List` focused and a row selected, do ⌥⌘↑ and ⌥⌘↓
reach menu-bar items carrying those key equivalents, or does the list consume them?

**Why it matters:** #0359 assigns Move Up to ⌥⌘↑ and Move Down to ⌥⌘↓.

### Build

Tab **0383 arrows** of #0380's spike app, plus **Spike ▸ Move Up** and **Spike ▸ Move Down**.

### Observe

1. Click row "c".
2. Press ⌥⌘↑ once, then ⌥⌘↓ once.

Record every new log line: `menu bar: ⌥⌘↑`, `menu bar: ⌥⌘↓`, and any `selection: …`.

### Pass or fail, and what changes

- **Pass** — each press logs its menu-bar line, and no `selection:` line appears. #0359 is unchanged.
- **Fail** — the selection moves (a `selection:` line appears, with or without the menu line), or
  nothing logs. #0359 changes Move Up and Move Down to ⌥⌘[ and ⌥⌘], the move-line-up/down pair many
  Mac editors use, and re-runs its shortcut-uniqueness test. Re-run this spike with those keys before
  accepting them.

## Expected behavior

- [ ] The observation is recorded here: versions, and the log lines from each press.
- [ ] #0359 is updated per the outcome.

## Notes

Parent: #0380. Guidance affected: #0359.

## Blocked on human observation — 2026-09-22

This spike needs a person at the Mac: pressing ⌥⌘↑ / ⌥⌘↓ with a list row selected cannot be
synthesized by an unattended round (no Accessibility permission; #0380 §Who runs it). Brennan
instructed autonomous operation on 2026-09-22 ("work autonomously like I am AFK"), so the
observation is deferred, not answered. **Unavoidably blocked on human input for the M7 quest.**

**Unblock:** relaunch the spike app (`cd build/spike-m7 && swift build && .build/debug/M7Spikes`),
tab **0383 arrows**, follow the Observe steps in #0380, record pass/fail here.
macOS 27.0, Xcode 27.0 (27A266a) at time of blocking.
