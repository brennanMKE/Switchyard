# 0198 — Decide whether State.init(ychar:) is dead code or a parser gap

| | |
|---|---|
| **Status** | resolved |
| **Module** | YardGit |
| **Milestone** | — |
| **Platform** | macOS |
| **First seen** | 2026-08-17 |

## Description

**Found by the M1 milestone review, 2026-08-17.** #0142 promised this issue and it was never filed.

`WorktreeStatusEntry.State.init?(ychar:)` — `YardKit/Sources/YardGit/WorktreeStatus.swift:68` — has no
caller in the engine. #0142's mutation R4 is recorded **GREEN** for exactly that reason, with the note:

> *"Deleting it, or wiring it where the parser actually handles the Y column, is a separate change and
> belongs in its own issue."*

This is that issue. It is a real fork, not a cleanup: either the parser should be using it for the
worktree (Y) column and does not, in which case something is under-parsed, or it is dead and should
go.

## Expected behavior

- [ ] Establish which it is by reading how the parser handles the Y column today, and record the answer
      in the issue before changing anything.
- [ ] If dead: delete it, and confirm no test referenced it (a test that referenced dead code was
      testing nothing).
- [ ] If it should be wired: wire it, with a test that distinguishes a Y-column state the parser
      currently gets wrong, plus the mutation that kills it.
- [ ] `swift test` prints a `Test run with N tests` line; state the delta once the fork is settled —
      it is 0 for a deletion and +1 or more for a wiring.

## Notes

The cheap answer is "delete it", and the reason to look first is that #0142's mutation went green,
which means **no test can tell the difference either way** — that is equally consistent with dead code
and with a gap in the parser.

## Work log

| Phase | Who | Cost |
|---|---|---|
| **Authoring** | Opus 5 | from the M1 milestone review's out-of-scope list, framed as a fork with the determination as the first deliverable |
| **Implementation** | Sonnet, Claude Code subagent | **89,968 tokens** measured, **$0.43** at $4.80/MTok combined |
| **Review** | Opus 5 | in-context; the determination re-checked and the separate finding verified independently |

| | |
|---|---|
| **Rounds** | 1 |
| **Wall time** | 7m51s of round time, plus review |
| **Suite** | 893 → 893 (a deletion; the flat count *is* the criterion) |
| **Branch** | `issue/0198` |
| **Commit** | `ecdc008` |

**The determination is the deliverable, and it was made from evidence rather than convenience.** "Delete
it" was the cheap answer and turned out to be right, but the round established *why*: the Y half of an
XY pair is drawn from two fixed alphabets per git's short-format table, and `?`, `!` and `u` are never
XY characters at all — they are standalone record-type leading tokens that `parse` dispatches through
separate branches *before* any XY field is read. The test file's own real captured conflict record shows
XY as `AA`. So wiring `init?(ychar:)` in would have produced a branch a real `git status` can never
reach — dead either way.

**And it found a real defect while looking, which is the argument for framing this as a fork rather
than a cleanup.** `State(char:)` has no case for `T` or `U`, and both call sites coalesce the nil to
`.unmodified` — so a typechange and the unmerged half of `AU`/`UD` report as **unmodified**. Verified
independently by the reviewer. Filed as **#0207**, not folded in: it is a different defect, and wiring
`ychar:` would not have fixed it.
