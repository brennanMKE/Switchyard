# 0294 — listHunks loses --unified=3 and merges two hunks into one

| | |
|---|---|
| **Status** | resolved |
| **Module** | YardGit |
| **Milestone** | M1 |
| **Platform** | macOS |
| **First seen** | 2026-08-18 |

## Description

**Finding 2 of the M1 milestone review, eighth pass, 2026-08-18.** `listHunks` loses `--unified=3`
undetected, and then reports **the wrong number of hunks**.

`YardKit/Sources/YardGit/Hunks.swift:378`, remove `"--unified=3"`: full suite green,
`Test run with 1103 tests in 78 suites passed after 66.071 seconds`.

**Measured against the engine**, with `diff.context = 10` and a file with two separated changes:

```
pristine:  PROBE-CONTEXT: fileDiffs=1 hunks=[2]
mutant:    PROBE-CONTEXT: fileDiffs=1 hunks=[1]
```

The two changes **merge into one hunk** — `@@ -1,30 +1,30 @@` instead of `@@ -2,7 +2,7 @@` and
`@@ -17,7 +17,7 @@`.

**Hunk identity is what `stage --hunk` and `commit --hunk` act on.** An agent asked to stage only the
first change stages both, silently. The flag is documented as load-bearing at `Hunks.swift:368` —
*"`--unified=3` (overrides `diff.context`)"* — and the documentation is the only thing enforcing it.

## Expected behavior

- [ ] A named test in `YardKit/Tests/YardGitTests/HunksTests.swift`: `diff.context` set high (10 is
      enough for the existing two-hunk fixture), asserting `listHunks` still reports **two** hunks for a
      file with two separated changes.
- [ ] **Verify the config takes effect** — `git diff` run without `--unified=3` in the fixture must show
      the merged single hunk. A `diff.context` git ignores pins nothing.
- [ ] Assert the hunk **headers**, not just the count, so a change that produces two hunks with wrong
      boundaries also dies.
- [ ] **Mutation:** drop `--unified=3`. The new test must redden.
- [ ] `cd YardKit && swift test` **twice, in the foreground with an explicit 900000 ms timeout**, both
      green, above the count in `docs/test-baseline.txt`.

## The files

`YardKit/Tests/YardGitTests/HunksTests.swift`. **No production change.**

## Notes

**Milestone M1**, one of four from the eighth pass. Sibling of **#0293**; with both resolved the
`listHunks` argument vector is genuinely pinned, which #0289 claimed prematurely.

`twoSeparatedEditsProduceTwoHunks` was believed to cover this indirectly. It does not: it runs at git's
default context, where the two edits are separate anyway.

## Work log

- **Round 1 — 2026-08-18, Sonnet subagent, 85,185 tokens, 14m58s, accepted.** One fixture helper, one
  test. 1106 → 1108 merged.
- **It confirmed the belief this issue overturned**, rather than just asserting it: under the mutation
  `twoSeparatedEditsProduceTwoHunks` stays **green**, which is the evidence that it never covered this.
- **It pins the merged shape from plain git first** — `@@ -1,20 +1,21 @@` at `diff.context=10` versus
  the two hunks at git's default — so a `diff.context` that failed to take effect would fail loudly.
- **Headers and boundaries, not just the count**, so a two-hunk result with wrong boundaries dies too.
- **Reviewed by re-running.** Full suite twice on the merged result, both `Test run with 1108 tests in
  78 suites passed`. Dropping the flag reddens twelve assertions across both ref formats.
- **The `listHunks` argument vector is now genuinely pinned** — `--no-renames` (#0267),
  `core.quotepath=false` and `--default-prefix` (#0283), `--no-ext-diff` and `--no-textconv` (#0289),
  `--no-color` (#0293), `--unified=3` (this). #0289's commit message claimed that a vector ago and was
  wrong; this time the claim is exhaustive against the actual argument list.
