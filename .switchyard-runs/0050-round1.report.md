Embed the switchyard binary in the app bundle

Added a PBXShellScriptBuildPhase to Switchyard.xcodeproj/project.pbxproj.

- Phase id: 0050E00D0050E00D0050A001, name "Embed switchyard CLI". Grepped the
  pbxproj for the id first: 0 hits. New `/* Begin PBXShellScriptBuildPhase section */`
  was inserted between PBXResourcesBuildPhase and PBXSourcesBuildPhase (alphabetical
  section order).
- Placement in the Switchyard target's buildPhases: after Sources/Frameworks/Resources,
  before the two BrokerAgent embed phases:
    Sources, Frameworks, Resources, Embed switchyard CLI,
    Embed Broker Agent Executable, Embed Broker Agent Plist
- shellPath = /bin/bash; runOnlyForDeploymentPostprocessing = 0 (runs every build);
  outputPaths = ("$(BUILT_PRODUCTS_DIR)/$(CONTENTS_FOLDER_PATH)/Resources/bin/switchyard");
  shellScript is the issue's transcription verbatim, pbxproj-escaped (\n, \", \\).

One extra change the issue did not transcribe (assumption, stated per rules): the first
build failed with `error: Sandbox: swift-package ... deny(1) file-read-data
.../YardKit` and `** BUILD FAILED **` because the project-level
ENABLE_USER_SCRIPT_SANDBOXING = YES sandboxes script phases, and this phase builds a
package (reads $SRCROOT/YardKit, writes a scratch tree). I set
ENABLE_USER_SCRIPT_SANDBOXING = NO on the two app-target configs only (Debug
26B64D5630251BCF00EB4FD0, Release 26B64D5730251BCF00EB4FD0). The project-level YES is
untouched, as are all four signing-related settings. No alternative declaration of
inputs/outputs can make `swift build` sandbox-clean.

Verification 1 — unsigned xcodebuild build:
  xcodebuild build -project Switchyard.xcodeproj -scheme Switchyard \
    -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO
  EXIT: 0, ** BUILD SUCCEEDED **. Script phase ran (log lines):
    PhaseScriptExecution Embed\ switchyard\ CLI .../Switchyard.build/Debug/Switchyard.build/Script-0050E00D0050E00D0050A001.sh (in target 'Switchyard' from project 'Switchyard')
    Building for debugging...
    [5/23] Compiling YardKit ServiceNames.swift
    [23/26] Compiling switchyard main.swift
    [24/26] Linking switchyard
    Build of product 'switchyard' complete! (6.21s)
    note: code signing disabled; embedded CLI left unsigned
  The "note: code signing disabled; embedded CLI left unsigned" line is the script's
  else-branch guard behaving correctly on this unsigned build; "note: skipping CLI
  embed for preview build" did NOT appear (not a preview build). The generated
  Script-0050...sh contains the transcribed script (grep for three distinctive
  lines: 3/3 found).

Verification 2 — the embedded binary (APP = $BUILT_PRODUCTS_DIR/Switchyard.app,
  BUILT_PRODUCTS_DIR from -showBuildSettings = .../DerivedData/Switchyard-bvzzwyisxcmyllfnkjdrawanhlyd/Build/Products/Debug):
  ls -l "$APP/Contents/Resources/bin/switchyard":
    -rwxr-xr-x@ 1 brennan staff 733648 Sep  3 02:33 .../Switchyard.app/Contents/Resources/bin/switchyard
  "$APP/Contents/Resources/bin/switchyard" --version  →  `switchyard 0.0.1`, exit 0
  otool -L (full output): only /usr/lib/libSystem.B.dylib, AppKit, CoreFoundation,
  Foundation, libobjc, and twenty-odd /usr/lib/swift/libswift*.dylib entries
  (libswiftCore, libswiftCoreFoundation, libswiftCoreImage, libswiftDispatch,
  libswiftIOKit, libswiftMetal, libswiftOSLog, libswiftObjectiveC,
  libswiftQuartzCore, libswiftSpatial, libswiftUniformTypeIdentifiers,
  libswiftXPC, libswift_Builtin_float, libswift_Concurrency,
  libswift_DarwinFoundation2, libswiftos, libswiftsimd — several weak).
  `otool -L ... | grep @rpath` printed NOTHING (grep exit 1) — no @rpath load
  command, so the sibling-project "Library not loaded" bug is absent.
  codesign -v --deep --strict: on this unsigned build there is nothing to check —
  the bundle is unsigned and that command proves nothing about a resource-sealed
  Mach-O anyway; not cited, not run as evidence.

Verification 3 — package suite unchanged:
  cd YardKit && swift test  →
    ✔ Test run with 1260 tests in 90 suites passed after 67.705 seconds.
  1260, unchanged — this issue adds no package code.

Verification 4 — no executable product dependency (the costs-a-day trap):
  grep -c "XCSwiftPackageProductDependency" Switchyard.xcodeproj/project.pbxproj
    → 8  (= 2 section begin/end markers + 6 isa lines, the same 6 pre-existing
      entries: YardGit, YardKit, YardUI, YardKit, YardCommands, SlidingTabs)
  grep "productName = switchyard" ...  → no output, exit 1.
  The app target's packageProductDependencies list is byte-identical to before
  (YardGit, YardKit, YardCommands, SlidingTabs). `switchyard` never enters
  Xcode's graph.

Not done: nothing else. Signing was never touched (no -allowProvisioningUpdates, no
signing setting changed); both builds ran with CODE_SIGNING_ALLOWED=NO
CODE_SIGNING_REQUIRED=NO. Working tree contains exactly one modified file:
Switchyard.xcodeproj/project.pbxproj. Logs kept in build/0050-xcodebuild.log,
build/0050-xcodebuild-2.log, build/0050-swift-test.log.
