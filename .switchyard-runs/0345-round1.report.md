Wire switchyard hunks into the command registry

Files changed:
- `YardKit/Sources/YardCommands/EngineCommands.swift` — top-level `case "hunks":`
  dispatching to a new `runHunks(arguments:workingDirectory:)` arm (54 lines added).
- `YardKit/Sources/YardKit/CommandRegistry.swift` — new `hunksSpec` (name "hunks",
  schemaName "hunks", flags --staged/--unstaged, exit codes 0/1/6, `payload: nil`
  with the statusSpec/conflictsSpec/wtSpec precedent comment) appended to
  `CommandRegistry.all`.
- `YardKit/Schemas/hunks.json` — regenerated via
  `SCHEMA_REGENERATE=1 swift test --filter checkedInSchemasMatchEmitter`; carries
  the self-reference form `"result": {"optional": true, "schema": "hunks"}`.
- `YardKit/Tests/YardKitTests/CommandRegistryTests.swift` — "exactly seven
  entries" → "exactly eight entries" (count and distinct-name assertions).
- `YardKit/Tests/YardCommandsTests/HunksCommandTests.swift` — NEW, six tests.

Encodable finding: `[FileDiff]` already conforms — `Hunk: Encodable` (Hunks.swift:482)
and `FileDiff: Encodable` (Hunks.swift:493) with explicit private `CodingKeys` enums,
pinned by DiffBlameWireTests since #0132. Step 2 of the pattern was already done;
changed nothing in Hunks.swift. The schema-binding test pins a fully-populated
FileDiff to exactly its six wire keys and a fully-populated Hunk to its eight.

Area argument parsing: the arm takes `arguments` (the full array, `arguments[0]`
== "hunks"), drops the first token, and requires the tail to be exactly one known
flag: `--staged` → `.staged`, `--unstaged` → `.unstaged`. Empty tail, two-plus
tokens, or any unknown token produce `EnvelopeFail(code: .usage, …)` on stdout
with the human-readable `[error] usage: …` line on stderr and exit `.usage` —
mirroring runYard's unknown-subcommand shape (CommandLineRunner.swift:88-90),
refused before any repository access, never a default guess. After the flag
parses: `WorktreeContext.resolve(path: workingDirectory)` (the passed path, never
`FileManager.default.currentDirectoryPath`), then `listHunks(at:area:)`, wrapped
in `Envelope(result: EncodableResult(result))`, exit `.success`; thrown errors →
`EnvelopeFail(code: .repositoryError)`, exit 6.

Test line: `Test run with 1192 tests in 86 suites passed after 82.489 seconds.`
Baseline 1186 → delta +6 (the six new test functions in HunksCommandTests).

Fixture-determined assertions: `FixtureRepository.linear()` commits a.txt ("a\n"),
b.txt, c.txt (Commit's default files). Writing "changed\n" over a.txt gives exactly
one FileDiff under `--unstaged` with one hunk, header "@@ -1 +1 @@", body exactly
["-a", "+changed"], non-empty id. After `GitProcess().run(["add", "a.txt"])`,
`--staged` reports the same hunk and `--unstaged` reports an empty array. Both
directions asserted.

Mutations (each applied, killed, reverted byte-exact; `git diff --stat` clean
between them and at the end):
1. Catch in runHunks returns a success envelope → killed by
   `HunksCommandTests/nonRepositoryPathReturnsRepositoryErrorAtExitSix`
   (3 issues: exitCode .success, ok true, error nil).
2. `case oldStart` given raw value "oldStartMutated" in Hunk's CodingKeys →
   killed by `HunksCommandTests/schemaResultIsTheSelfReferenceAndTheTypesEncodeOnlyTheirCodingKeys`
   and `DiffBlameWireTests/hunkEncodesToTheLiteralWireShapeWithItsDerivedID`
   (byte pin shows "oldStartMutated":1).
3. hunksSpec removed from `CommandRegistry.all` → killed by
   `HunksCommandTests/hunksSpecIsRegisteredWithRequiredMetadata` (lookup nil)
   and `CommandRegistryTests/"registry reports exactly eight entries"` (count 7).

Verification: full `swift test` from YardKit on the fully reverted tree —
`Test run with 1192 tests in 86 suites passed after 82.489 seconds.` Run twice
total (after implementation, after mutations); both green.

Not done: nothing from the issue's checklist. One note — a first
`--filter "hunks engine arm"` invocation matched 0 tests (swift-testing filter
did not match the suite display name); reran with `--filter HunksCommandTests`,
which is what all mutation runs used.
