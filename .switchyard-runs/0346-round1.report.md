Wire switchyard log into the command registry

Files changed (all uncommitted, on issue/0346):
- YardKit/Sources/YardCommands/EngineCommands.swift — top-level `case "log":`
  in runEngineCommand dispatching to a new private `runLog(arguments:workingDirectory:)`,
  modeled on runHunks (#0345). Argument tail = Array(arguments.dropFirst()); any
  `-`-prefixed token anywhere in the tail is refused with
  EnvelopeFail(code: .usage) + "[error] usage:" on stderr + exit .usage BEFORE any
  repository access. Otherwise: WorktreeContext.resolve(path: workingDirectory) for
  the passed directory (never FileManager.default.currentDirectoryPath), then
  CommitLog.run(path:rangeArguments:) with the tail (empty = engine default HEAD),
  Envelope(result: EncodableResult(result)), exit .success; thrown errors →
  EnvelopeFail(code: .repositoryError) exit 6. CommitLogOptions untouched — no flags
  on this surface.
- YardKit/Sources/YardKit/CommandRegistry.swift — logSpec added (name "log",
  non-empty summary, schemaName "log", flags: [], exit codes 0/1/6, payload: nil
  with the statusSpec/conflictsSpec/wtSpec/wtWhereSpec/hunksSpec precedent comment:
  array-of-objects result, PayloadShape is flat-only #0194) and appended to
  CommandRegistry.all.
- YardKit/Tests/YardKitTests/CommandRegistryTests.swift — "exactly eight entries"
  → "exactly nine entries" (count and distinct-names both 9).
- YardKit/Schemas/log.json — regenerated via SCHEMA_REGENERATE=1 swift test
  --filter checkedInSchemasMatchEmitter (passed; also passes without the flag).
- YardKit/Tests/YardCommandsTests/LogCommandTests.swift — NEW, 6 tests, all
  swift-testing: not-a-repository (exit .repositoryError, error.code
  "repository_error"); unknown-flag refusal asserted in BOTH an empty temp dir and
  a real FixtureRepository (refusal is argument-shaped, not repository-shaped;
  stderr carries "[error] usage:"); default listing = exactly 3 entries, oids
  [c, b, a] (CommitLog.run is newest-first, CommitLog.swift:231-234) with parent
  chains [oid_b]/[oid_a]/[] and messages "c\n"/"b\n"/"a\n" (git commit -m's
  trailing newline, %B verbatim), author "Fixture", signatureStatus "noSig";
  range case ["<oid-b>..HEAD"] → exactly 1 entry (c); registry spec test
  (lookup "log", schemaName "log", exit codes contain 0, 1, 6); schema binding
  (Schemas/log.json envelope.success.result carries "schema": "log" and no
  "fields"; a fully-populated CommitLogEntry + Trailer encodes exactly its seven /
  two CodingKeys with sortedKeys).

Encodable finding: CommitLogEntry, Trailer, and SignatureStatus already conform
to Encodable with explicit CodingKeys (CommitLog.swift:409, 437, 449-458), pinned
byte-for-byte by LogGraphWireTests (#0133). Step 2 of the #0115 pattern was already
done — changed nothing there.

Argument-refusal rule: no option flags in this issue. Zero or more range
arguments pass through; any flag-shaped token is a usage refusal (exit 1) before
repository access — never silently ignored, never a default guess.

Test line: `Test run with 1198 tests in 87 suites passed after 58.999 seconds.`
Baseline at dispatch (docs/test-baseline.txt / commit d70ad48 message): 1192.
Delta: +6 (the six new LogCommandTests).

Mutations (each applied, killed, reverted byte-exact — git diff --stat on
CommitLog.swift prints nothing after revert):
1. Break the not-a-repository path (runLog catch returns a success envelope) →
   killed by LogCommandTests.nonRepositoryPathReturnsRepositoryErrorAtExitSix
   (exit .success != .repositoryError, ok true != false).
2. Rename one CommitLogEntry CodingKeys case (`case oid = "commit_oid"`) →
   killed by LogCommandTests.schemaResultIsTheSelfReferenceAndTheTypesEncodeOnly
   TheirCodingKeys (key set mismatch) AND LogGraphWireTests byte pins
   (commitLogEntryFullValueEncodesToTheLiteralWireShape,
   rootCommitEncodesEmptyParentsAndTrailersAsEmptyArrays,
   envelopeWrapsLogAndGraphPayloads).
3. Remove logSpec from CommandRegistry.all → killed by
   LogCommandTests.logSpecIsRegisteredWithRequiredMetadata (lookup nil) and
   CommandRegistryTests "registry reports exactly nine entries" (count 8 != 9).

Nothing not done to my knowledge: all Expected-behavior boxes addressed; the
final full suite ran on the fully reverted (post-mutation) tree in the
foreground with a 900000 ms timeout and passed.
