Wire switchyard conflicts into the command registry

Verified and kept the prior session's work: the `case "conflicts":` arm plus
`runConflicts(workingDirectory:)` in EngineCommands.swift (resolves
`WorktreeContext` for the PASSED directory, never `FileManager.default.currentDirectoryPath`;
failures become `EnvelopeFail(code: .repositoryError, ...)`, exit 6), the
`conflictsSpec` in `CommandRegistry.all` (payload: nil, citing the #0225/#0194
flat-only precedent), and the CommandRegistryTests bump to "exactly five entries".

Step 2 already conformed, changed nothing: `ConflictedFile.CodingKeys` is
`case path, kind, base, ours, theirs` and `StageEntry.CodingKeys` is
`case oid, mode` in Conflicts.swift (pathBytes deliberately not on the wire,
#0129 Decision 6). Conflicts.swift was not modified at any point; after the
mutation round `git diff` on it is empty.

New: YardKit/Tests/YardCommandsTests/ConflictsCommandTests.swift (154 lines,
swift-testing, modeled on StatusCommandTests): four tests —
- nonRepositoryPathReturnsRepositoryErrorAtExitSix (empty temp dir → exit 6,
  ok == false, error.code == "repository_error")
- conflictedFixtureReportsOneUUPathWithAllThreeStages (FixtureRepository.conflicted:
  exactly one path f.txt, kind "UU", base/ours/theirs all present with
  mode 100644 and non-empty oid, exit .success, ok, schemaVersion 1)
- conflictsSpecIsRegisteredWithRequiredMetadata (lookup via try #require,
  non-empty summary, schemaName "conflicts", codes 0 and 6)
- schemaResultIsTheSelfReferenceAndTheTypeEncodesOnlyItsFiveKeys (step 6:
  conflicts.json self-reference form `{"schema": "conflicts"}`, no `fields`;
  a fully-populated ConflictedFile encodes exactly path/kind/base/ours/theirs,
  no pathBytes, stages exactly oid/mode)

Step 5 verified: backed up Schemas/conflicts.json, ran
`SCHEMA_REGENERATE=1 swift test --filter checkedInSchemasMatchEmitter`,
diffed — byte-identical; the filter also passed (1 test).

Mutations (each applied, killed, reverted byte-exact, tree re-checked):
1. Catch in runConflicts returns a success envelope with an empty array →
   killed by nonRepositoryPathReturnsRepositoryErrorAtExitSix (exitCode/ok/error all wrong).
2. `case path` → `case path = "p"` in Conflicts.swift CodingKeys → NOT a build
   error, as predicted: private CodingKeys compiles. Killed by 5 tests: my
   schemaResultIsTheSelfReferenceAndTheTypeEncodesOnlyItsFiveKeys (key set got
   "p") and conflictedFixtureReportsOneUUPathWithAllThreeStages, plus
   StatusConflictsWireTests' conflictedFileFullValueEncodesToTheLiteralWireShape,
   conflictParserOutputForANonUTF8ConflictEncodesLossily, envelopeWrapsStatusAndConflictsPayloads.
3. conflictsSpec removed from CommandRegistry.all → killed by
   conflictsSpecIsRegisteredWithRequiredMetadata (`try #require(lookup) → nil`)
   and "registry reports exactly five entries".

Verification: `cd YardKit && swift test` foreground →
`Test run with 1178 tests in 83 suites passed after 84.238 seconds.`
Baseline 1174 → delta +4 (the four new tests). Line present, zero failures.

Could not do: nothing outstanding. Note the second grep for the summary line
re-ran the suite once; both runs printed 1178 passed.
