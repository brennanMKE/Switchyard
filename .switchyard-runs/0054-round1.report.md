Write the manual XPC verification script

Delivered `scripts/verify-xpc.md` per the 2026-08-17 planning update: prose with
copy-pasteable commands, one section per scenario with preconditions, exact
commands, expected stdout shape, and exit codes cited by `ExitCode` case name
(verified against `YardKit/Sources/YardKit/ExitCode.swift`: 2 = `.brokerUnreachable`
"broker_unreachable", 3 = `.appUnavailable` "app_unavailable", 5 =
`.sessionTerminated` "session_terminated").

Machine state, checked read-only before running anything: agent NOT registered
(`launchctl print gui/$UID/co.sstools.Switchyard.broker` → "Could not find service
"co.sstools.Switchyard.broker" in domain for user gui: 501"), app not running
(`pgrep -lx Switchyard` → no match), no installed app bundle (DerivedData build
products only), no `/usr/local/bin/switchyard`.

RUN vs UNRUN: 1 RUN, 7 UNRUN, cold-start UNRUN.

- Scenario 1 (nothing registered) — RUN. Built the CLI with
  `swift build --product switchyard` (YardKit, 5.75s) and ran it. `whereami`
  observed: exit 2, stdout
  `{"error":{"code":"broker_unreachable","message":"cannot reach the broker: Couldn’t communicate with a helper application."},"ok":false,"schemaVersion":1}`,
  stderr `[error] broker_unreachable: …` (both pasted into the doc).
- Discrepancy found: the planning update's scenario-1 command `switchyard noop`
  exits 0, not 2 — `noop` is in `localCommandNames` (CommandLineRunner.swift:16)
  and never touches XPC. Observed output `{"ok":true,"schemaVersion":1}`, exit 0,
  recorded in the doc; the scenario uses `whereami` as the exit-2 probe.
- Scenarios 2-8 and cold-start — UNRUN. Reason common to all: they need the app
  installed and/or the launch agent registered and/or the app launched, all
  forbidden this round (never launch the app, never register launch agents, no
  state-changing `launchctl`). Scenario 2 has a second, independent blocker:
  no CLI subcommand reaches `BrokerProtocol.brokerPing` today
  (`BrokerConnection.ping()` exists; nothing in `CommandRegistry.all` calls it).
  Scenario 3 Part B: `--no-launch` is not parsed by the CLI — it is a parameter
  on `AppConnection.connect` (AppConnection.swift:47-51); guide §10 lists the
  flag as pending. Each UNRUN section states its expected shape derived from the
  source, labeled as not-observed.
- Inspection finding recorded in scenario 4: the app's `perform` forwards to
  `performCommand` → `runYard` (AppXPCServer.swift:240, CommandLineRunner.swift:136),
  which answers remote commands with the usage envelope exit 1 —
  `runEngineCommand` (YardCommands/EngineCommands.swift:11) has no caller in the
  app target. Over-the-wire `whereami` would return "Unknown subcommand
  'whereami'" until the engine arm is wired into the app's XPC server.

Dropped scenarios, stated in the doc: app-pushed events and streaming progress
(RemoteControl's long-lived-session model; guide §11 decision 15 argv-in/
envelope-out; #0213 closed wontfix → sessions are #0349/M4), and CLI killed
mid-session (no session exists, so nothing to observe beyond the process dying).

Not done: criterion 4 (referencing the script from issues/Issues.md). Editing
the shared tracker file was outside this round's dispatch list; leave it to the
reviewer.

Suite untouched by this round, confirmed: "Test run with 1260 tests in 90
suites passed after 58.706 seconds."
